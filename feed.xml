<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://ruyshy.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://ruyshy.github.io/" rel="alternate" type="text/html" /><updated>2025-04-23T10:07:48+09:00</updated><id>https://ruyshy.github.io/feed.xml</id><title type="html">ruyshy blog</title><subtitle></subtitle><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><entry><title type="html">Assembly 실습 환경 설정</title><link href="https://ruyshy.github.io/assembly/assembly2-setting/" rel="alternate" type="text/html" title="Assembly 실습 환경 설정" /><published>2025-04-22T00:00:00+09:00</published><updated>2025-04-22T00:00:00+09:00</updated><id>https://ruyshy.github.io/assembly/assembly2-setting</id><content type="html" xml:base="https://ruyshy.github.io/assembly/assembly2-setting/"><![CDATA[<p>assembly #2</p>

<h2 id="1-sasm">1. SASM</h2>

<p><strong>SASM ( SimpleASM</strong> 의 약자 )은 <a href="https://en.wikipedia.org/wiki/Netwide_Assembler">NASM</a> , <a href="https://en.wikipedia.org/wiki/MASM">MASM</a> , <a href="https://en.wikipedia.org/wiki/GNU_Assembler">GAS</a> , <a href="https://en.wikipedia.org/wiki/FASM">FASM</a> 어셈블리 언어를 위한 무료 <a href="https://en.wikipedia.org/wiki/Open_source">오픈 소스</a> 크로스 플랫폼 <a href="https://en.wikipedia.org/wiki/Integrated_development_environment">통합 개발 환경</a> 입니다 . 구문 강조 기능과 디버거가 포함되어 있습니다.</p>

<h3 id="1-1-sasm-설치하기">1-1. SASM 설치하기</h3>

<p>sasm 공식 홈페이지 : <a href="https://dman95.github.io/SASM/english.html">https://dman95.github.io/SASM/english.html</a></p>

<p>해당 홈페이지에 접속하여 설치파일 다운로드 후, 설치를 진행합니다.</p>

<h3 id="1-2-sasm-setting">1-2. SASM Setting</h3>

<p><img src="/assets/images/assembly/2025-04-22-assembly2-setting/image1.png" alt="image1.png" style="max-width:100%;" /></p>

<p>nasm x64 기준으로 진행 합니다.</p>

<p>설치 후, 설치 경로인 \SASM\Projects 폴더 안에 여러 예제를 테스트 할 수 있습니다.</p>

<p>File→Open 메뉴를 가보면 자동으로 \SASM\Projects 경로로 기본 설정 되어 있습니다.</p>

<p>NASM 위주로 학습을 진행할 예정이니, <code class="language-plaintext highlighter-rouge">NASMHellox64.asm</code> 를 살펴 봅시다.</p>

<h3 id="1-3-nasmhellox64asm">1-3. NASMHellox64.asm</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">%include "io64.inc"
</span>
<span class="nf">section</span> <span class="nv">.data</span>
    <span class="nf">msg</span> <span class="nv">db</span> <span class="s">'Hello, world!'</span><span class="p">,</span> <span class="mi">0</span>

<span class="nf">section</span> <span class="nv">.text</span>
<span class="nf">global</span> <span class="nv">main</span>
<span class="nl">main:</span>
    <span class="nf">mov</span> <span class="nb">rbp</span><span class="p">,</span> <span class="nb">rsp</span> <span class="c1">; for correct debugging</span>
    <span class="nf">PRINT_STRING</span> <span class="nv">msg</span> <span class="c1">; print hello world</span>
    <span class="nf">NEWLINE</span> <span class="c1">; print newline</span>
    <span class="nf">xor</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rax</span> <span class="c1">; set rax to 0</span>
    <span class="nf">ret</span> <span class="c1">; return</span>
</code></pre></div></div>

<p>단축키 F9(Build and run)를 눌러 테스트를 진행할 수 있습니다.</p>

<p><img src="/assets/images/assembly/2025-04-22-assembly2-setting/image2.png" alt="image2.png" style="max-width:100%;" /></p>

<p>단축키 F5(Debug)를 눌러보면</p>

<p><img src="/assets/images/assembly/2025-04-22-assembly2-setting/image3.png" alt="image3.png" style="max-width:100%;" /></p>

<p>저처럼 memory와 registers를 볼 수 있도록</p>

<p>Debug→Show Memory, Show Registers 설정을 on으로 설정합시다!</p>

<h3 id="1-4-코드-흐름-살펴보기">1-4. 코드 흐름 살펴보기</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">%include "io64.inc"
</span></code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">io64.inc</code> 파일을 include 한다는 뜻.</li>
  <li>이 안에는 <code class="language-plaintext highlighter-rouge">PRINT_STRING</code>, <code class="language-plaintext highlighter-rouge">NEWLINE</code> 같은 매크로(편의 함수)가 정의되어 있음.</li>
  <li><strong>즉, 직접 WriteFile 같은 API 호출 안 해도</strong> 편하게 출력 가능하게 만든 것.</li>
</ul>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">section</span> <span class="nv">.data</span>
    <span class="nf">msg</span> <span class="nv">db</span> <span class="s">'Hello, world!'</span><span class="p">,</span> <span class="mi">0</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">.data</code> 섹션: 데이터를 정의하는 곳.</li>
  <li><code class="language-plaintext highlighter-rouge">msg</code>라는 이름으로 문자열 저장.</li>
  <li>문자열 끝에 <code class="language-plaintext highlighter-rouge">0</code> (NULL) 추가. (C 스타일 문자열)</li>
</ul>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">section</span> <span class="nv">.text</span>
<span class="nf">global</span> <span class="nv">main</span>
<span class="nl">main:</span>
    <span class="nf">mov</span> <span class="nb">rbp</span><span class="p">,</span> <span class="nb">rsp</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">.text</code> 섹션: 실행되는 코드</li>
  <li><code class="language-plaintext highlighter-rouge">global main</code>: 프로그램 진입점을 <code class="language-plaintext highlighter-rouge">main</code> 함수로 지정 (링커가 main 찾게)</li>
  <li><code class="language-plaintext highlighter-rouge">mov rbp, rsp</code>: 디버깅할 때 스택 프레임 만들려고 넣는 것. (사실 없어도 돌아가긴 함)</li>
</ul>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="nf">PRINT_STRING</span> <span class="nv">msg</span>
    <span class="nf">NEWLINE</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">PRINT_STRING msg</code>: <code class="language-plaintext highlighter-rouge">msg</code> 내용을 콘솔에 출력 (io64.inc 매크로 덕분)</li>
  <li><code class="language-plaintext highlighter-rouge">NEWLINE</code>: 줄 바꿈 출력</li>
</ul>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="nf">xor</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rax</span>
    <span class="nf">ret</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">xor rax, rax</code>: rax 레지스터를 0으로 세팅 (종료코드 0)</li>
  <li><code class="language-plaintext highlighter-rouge">ret</code>: 함수 리턴 → 프로그램 종료</li>
</ul>

<h2 id="2-sasm-사용-방법">2. SASM 사용 방법</h2>

<h3 id="2-1-디버깅-기본-단축키">2-1. 디버깅 기본 단축키</h3>

<table>
  <thead>
    <tr>
      <th>기능</th>
      <th>단축키</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>디버깅 시작</td>
      <td>F5</td>
      <td>프로그램 실행하면서 디버깅 모드 진입</td>
    </tr>
    <tr>
      <td>브레이크 포인트 설정/해제</td>
      <td>F8</td>
      <td>해당 줄 멈추기 (줄 번호 클릭하거나 F8)</td>
    </tr>
    <tr>
      <td>계속 실행</td>
      <td>F5</td>
      <td>다음 브레이크 포인트까지 실행</td>
    </tr>
    <tr>
      <td>일시정지</td>
      <td>F5</td>
      <td>현재 실행 멈춤 (무한 루프 걸렸을 때 유용)</td>
    </tr>
    <tr>
      <td>Step Into</td>
      <td>F11</td>
      <td>다음 명령어로 이동 + 함수 안으로 들어감</td>
    </tr>
    <tr>
      <td>Step Over</td>
      <td>F10</td>
      <td>다음 명령어로 이동 + 함수는 건너뜀</td>
    </tr>
  </tbody>
</table>

<h3 id="2-2-디버깅-창-단축키">2-2. 디버깅 창 단축키</h3>

<table>
  <thead>
    <tr>
      <th>창 이름</th>
      <th>단축키</th>
      <th>기능</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>레지스터(Register) 창</td>
      <td>Ctrl+R</td>
      <td>CPU 레지스터(<code class="language-plaintext highlighter-rouge">RAX</code>, <code class="language-plaintext highlighter-rouge">RBP</code>, <code class="language-plaintext highlighter-rouge">RSP</code> 등) 보기</td>
    </tr>
    <tr>
      <td>메모리(Memory) 창</td>
      <td>Ctrl+M</td>
      <td>주소 또는 변수 메모리 직접 보기</td>
    </tr>
  </tbody>
</table>

<p>해당 레지스터 창, 메모리 창은 디버깅할 때, 매우 중요하므로 켜두는게 좋음!</p>

<p>더 자세히 알고싶다면, F1을 눌러 Help를 읽거나 sasm 문서를 찾아보자!</p>

<h3 id="2-3-메모리변수-보는-방법">2-3. 메모리/변수 보는 방법</h3>

<ul>
  <li>Memory 창에서
    <ul>
      <li>“Add Variable…“에 변수 이름 입력</li>
      <li>
        <p>타입 지정:</p>

        <p><code class="language-plaintext highlighter-rouge">b</code> = byte(1바이트)</p>

        <p><code class="language-plaintext highlighter-rouge">w</code> = word(2바이트)</p>

        <p><code class="language-plaintext highlighter-rouge">d</code> = double word(4바이트)</p>

        <p><code class="language-plaintext highlighter-rouge">q</code> = quad word(8바이트)</p>
      </li>
    </ul>
  </li>
  <li>주소 직접 보고 싶으면 “Address” 체크</li>
  <li>배열로 보면 “Array Size” 설정 가능</li>
</ul>

<p><strong>예시</strong>:</p>

<ul>
  <li>변수 주소: <code class="language-plaintext highlighter-rouge">msg</code></li>
  <li>명령어: <code class="language-plaintext highlighter-rouge">msg, b</code></li>
  <li>배열로 보면 <code class="language-plaintext highlighter-rouge">"array size" = 13</code> 설정 (Hello, world! 길이)</li>
</ul>

<h3 id="2-4-sasm-디버깅-연습-미션풀이">2-4. SASM 디버깅 연습 미션&amp;풀이</h3>

<details>
<summary>미션 1.</summary>
<div>

    <p><code class="language-plaintext highlighter-rouge">msg</code> 문자열이 메모리에 어떻게 저장되어 있는지 확인하기.</p>

    <p>목표:</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">msg</code> 데이터가 메모리에 어떤 형태로 들어가 있는지 실제로 확인</li>
      <li>“Hello, world!” 문자열이 1바이트 씩 저장된 걸 눈으로 확인하기</li>
    </ul>

    <p><strong>체크 포인트</strong>:</p>

    <ul>
      <li>‘H’, ‘e’, ‘l’, ‘l’, ‘o’, … 문자들이 차례로 들어가 있나?</li>
    </ul>

  </div>
</details>
<p><br /></p>

<details>
<summary>미션 1. 풀이</summary>
<div>

    <ol>
      <li>
        <p>F5(Debug)를 눌러 디버깅 진행</p>
      </li>
      <li>
        <p>F10(Step Over)를 통해 <code class="language-plaintext highlighter-rouge">PRINT_STRING msg ;</code> 까지 디버깅 하기.</p>

        <p><img src="/assets/images/assembly/2025-04-22-assembly2-setting/image4.png" alt="image4.png" style="max-width:100%;" /></p>
      </li>
      <li>
        <p>Memory 창에서 Add variable…을 눌러 msg 추가하고, Type은 Smart, b, 14 설정하면 “Hello, world!” 가 표현되는 것을 확인할 수 있다. (스페이스(공백)과 마지막에 0을 합친 문자열 개수가 14개)</p>
      </li>
      <li>
        <p>Type을 Hex로 변경해보면 <code class="language-plaintext highlighter-rouge">{0x48, 0x65, 0x6c, 0x6c, 0x6f, 0x2c, 0x20, 0x77, 0x6f, 0x72, 0x6c,0x64, 0x21, 0x0}</code> 해당 16진수인 값이 나오는데 아래의 아스키 코드표를 보면 H(0x48)을 확인할 수 있다. 해당값은 아스키코드 16진수 값이다!</p>

        <p><img src="/assets/images/assembly/2025-04-22-assembly2-setting/image5.png" alt="image5.png" style="max-width:100%;" /></p>
      </li>
      <li>
        <p>마지막에 0(0x00)을 넣어준 이유는 문자열의 끝을 알기 위해서 넣어준 것이다!</p>
      </li>
      <li>
        <p>즉, <code class="language-plaintext highlighter-rouge">msg db 0x48, 0x65, 0x6c, 0x6c, 0x6f, 0x2c, 0x20, 0x77, 0x6f, 0x72, 0x6c, 0x64, 0x21, 0x00</code> 이렇게 선언한 것과 <code class="language-plaintext highlighter-rouge">msg db 'Hello, world!', 0</code> 이렇게 선언한 것은 똑같다!</p>
      </li>
    </ol>

  </div>
</details>
<p><br /></p>

<details>
<summary>미션 2.</summary>
<div>

    <p><code class="language-plaintext highlighter-rouge">xor rax, rax</code> 명령어 수행 전후 <code class="language-plaintext highlighter-rouge">RAX</code> 값 비교하기</p>

    <p><strong>목표</strong>:</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">xor rax, rax</code> 명령어가 정확히 어떤 효과를 내는지 체감</li>
      <li>명령어 수행 전후 레지스터 변화를 관찰</li>
    </ul>

    <p><strong>체크 포인트</strong>:</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">RAX</code> 값이 0으로 깔끔하게 초기화됐나?</li>
    </ul>

  </div>
</details>
<p><br /></p>

<details>
<summary>미션 2. 풀이</summary>
<div>

    <ul>
      <li><strong>F5 (Debug)</strong> 눌러 디버깅 모드 진입</li>
      <li><strong>F10 (Step Over)</strong> 여러 번 눌러서 <code class="language-plaintext highlighter-rouge">xor rax, rax</code> <strong>직전</strong>까지 이동</li>
      <li><code class="language-plaintext highlighter-rouge">xor rax, rax</code> 줄에서 <strong>브레이크포인트 (F8)</strong> 설정</li>
      <li><strong>Ctrl+R (Registers 창)</strong> 열어서 레지스터 상태 확인
        <ul>
          <li>
            <p>이 시점에서는 <code class="language-plaintext highlighter-rouge">RAX</code>가 0이 아닐 수 있음</p>
          </li>
          <li>
            <p>예를 들면, <code class="language-plaintext highlighter-rouge">RAX = 0x000000000000000d</code> (13, 문자열 길이)처럼 되어 있을 수도 있다.</p>

            <p>(왜냐면 <code class="language-plaintext highlighter-rouge">PRINT_STRING</code> 매크로가 내부에서 <code class="language-plaintext highlighter-rouge">RAX</code>를 수정했을 수 있으니까)</p>
          </li>
        </ul>
      </li>
      <li>
        <p><strong>F11 (Step Into)</strong> 눌러 <code class="language-plaintext highlighter-rouge">xor rax, rax</code> 명령어 한 줄 실행</p>
      </li>
      <li>다시 <strong>Registers 창</strong> 확인
        <ul>
          <li>이제 <code class="language-plaintext highlighter-rouge">RAX = 0x0000000000000000</code> 으로 초기화되어 있어야 한다.</li>
        </ul>

        <p><strong>원리 설명</strong></p>

        <ul>
          <li>
            <p><code class="language-plaintext highlighter-rouge">xor rax, rax</code>는 <strong>RAX를 자기 자신과 XOR</strong> 연산하는 것</p>
          </li>
          <li>
            <p><strong>어떤 값이든 자기 자신과 XOR 하면 무조건 0이 된다</strong></p>

            <ul>
              <li>수학적으로: <code class="language-plaintext highlighter-rouge">x xor x = 0</code></li>
            </ul>
          </li>
          <li>
            <p>그래서 <code class="language-plaintext highlighter-rouge">xor rax, rax</code>는 매우 빠르고 최적화된 <strong>레지스터 클리어 방법</strong>이다.</p>

            <ul>
              <li><code class="language-plaintext highlighter-rouge">mov rax, 0</code> 보다 사이즈가 작고 CPU에 부담도 덜 준다.</li>
            </ul>
          </li>
        </ul>
      </li>
    </ul>

  </div>
</details>
<p><br /></p>

<h2 id="3-assembly-변수-선언-방법">3. Assembly 변수 선언 방법</h2>

<table>
  <thead>
    <tr>
      <th>구문</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">db</code> (define byte)</td>
      <td>1바이트 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">dw</code> (define word)</td>
      <td>2바이트 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">dd</code> (define double word)</td>
      <td>4바이트 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">dq</code> (define quad word)</td>
      <td>8바이트 변수</td>
    </tr>
  </tbody>
</table>

<p><strong>메모리에 직접 값을 “저장”하는 것</strong>으로 변수처럼 사용(64bit 기준에서는 <code class="language-plaintext highlighter-rouge">dq</code>를 많이 사용)</p>

<h3 id="3-1-예시">3-1. 예시</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">section</span> <span class="nv">.data</span>
    <span class="nf">a</span> <span class="nv">db</span> <span class="mi">10</span>       <span class="c1">; 1바이트짜리 변수 a = 10</span>
    <span class="nf">b</span> <span class="nv">dw</span> <span class="mi">1000</span>     <span class="c1">; 2바이트짜리 변수 b = 1000</span>
    <span class="nf">c</span> <span class="nv">dd</span> <span class="mh">12345678h</span> <span class="c1">; 4바이트짜리 변수 c = 0x12345678</span>
    <span class="nf">d</span> <span class="nv">dq</span> <span class="mh">123456789ABCDEF0h</span> <span class="c1">; 8바이트짜리 변수 d = 0x123456789ABCDEF0</span>

</code></pre></div></div>

<h3 id="3-2-중요한-포인트">3-2. 중요한 포인트</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">a</code>, <code class="language-plaintext highlighter-rouge">b</code>, <code class="language-plaintext highlighter-rouge">c</code>, <code class="language-plaintext highlighter-rouge">d</code>는 전부 <strong>메모리에 존재하는 이름(label)</strong>.</li>
  <li>변수 자체가 아니라, <strong>“그 메모리 위치”를 가리키는 라벨</strong>.</li>
  <li>값을 바꾸고 싶으면 메모리에 직접 접근해서 수정해야 함.</li>
</ul>

<h3 id="3-3-참고-uninitialized-변수">3-3. 참고: uninitialized 변수</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">section</span> <span class="nv">.bss</span>
    <span class="nf">buffer</span> <span class="nv">resb</span> <span class="mi">64</span> <span class="c1">; 64바이트짜리 빈 공간 (초기값 없음)</span>
</code></pre></div></div>

<h2 id="4-빅-엔디안-vs-리틀-엔디안">4. 빅 엔디안 vs 리틀 엔디안</h2>

<p><strong>엔디안(Endian)</strong> 은 <strong>“메모리에 데이터를 저장할 때, 바이트 순서를 어떻게 할 것인가”</strong> 를 정하는 규칙이다.</p>

<h3 id="4-1-리틀-엔디안-little-endian">4-1. 리틀 엔디안 (Little Endian)</h3>

<ul>
  <li><strong>“작은 게 먼저 저장”</strong> 된다.</li>
  <li>숫자의 <strong>가장 하위 바이트(Least Significant Byte)</strong> 가 먼저 온다.</li>
  <li><strong>x86, x86-64 (Windows PC)</strong> 는 리틀 엔디안 방식을 사용한다.</li>
</ul>

<p><strong>예시</strong></p>

<p>32bit 숫자 0x12345678을 저장한다고 하면:</p>

<table>
  <thead>
    <tr>
      <th>주소</th>
      <th>값</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0x00</td>
      <td>0x78</td>
    </tr>
    <tr>
      <td>0x01</td>
      <td>0x56</td>
    </tr>
    <tr>
      <td>0x02</td>
      <td>0x34</td>
    </tr>
    <tr>
      <td>0x03</td>
      <td>0x12</td>
    </tr>
  </tbody>
</table>

<p><strong>※ 가장 오른쪽(가장 작은 값)부터 거꾸로 저장!</strong></p>

<h3 id="4-2-빅-엔디안-big-endian">4-2. 빅 엔디안 (Big Endian)</h3>

<ul>
  <li><strong>“큰 게 먼저 저장”</strong> 된다.</li>
  <li>숫자의 <strong>가장 상위 바이트(Most Significant Byte)</strong> 가 먼저 온다.</li>
  <li>네트워크 통신 규약(Internet Protocol, TCP/IP) 등에서는 빅 엔디안을 사용한다.</li>
</ul>

<p><strong>예시</strong></p>

<p>32bit 숫자 0x12345678을 저장하면:</p>

<table>
  <thead>
    <tr>
      <th>주소</th>
      <th>값</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0x00</td>
      <td>0x12</td>
    </tr>
    <tr>
      <td>0x01</td>
      <td>0x34</td>
    </tr>
    <tr>
      <td>0x02</td>
      <td>0x56</td>
    </tr>
    <tr>
      <td>0x03</td>
      <td>0x78</td>
    </tr>
  </tbody>
</table>

<p><strong>※ 자연스럽게 왼쪽에서 오른쪽으로 읽는 느낌!</strong></p>

<h3 id="4-3-리틀-vs-빅-간단-비교">4-3. 리틀 vs 빅 간단 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>리틀엔디안</th>
      <th>빅엔디안</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>저장 순서</td>
      <td>가장 작은 바이트 → 가장 큰 바이트</td>
      <td>가장 큰 바이트 → 가장 작은 바이트</td>
    </tr>
    <tr>
      <td>사용 예시</td>
      <td>x86 CPU (PC), ARM(선택가능)</td>
      <td>네트워크 프로토콜, 일부 ARM 장치</td>
    </tr>
    <tr>
      <td>메모리 디버깅할 때</td>
      <td>값을 거꾸로 읽어야 함</td>
      <td>값을 순서대로 읽음</td>
    </tr>
  </tbody>
</table>

<h3 id="4-4-한-번-더-비교">4-4. 한 번 더 비교</h3>

<p>리틀엔디안:</p>

<p>0x12345678  →  메모리: 78 56 34 12</p>

<p>빅엔디안:</p>

<p>0x12345678  →  메모리: 12 34 56 78</p>

<h3 id="4-5-요약-한방">4-5. 요약 한방</h3>

<ul>
  <li>어셈블리어에서는 <strong><code class="language-plaintext highlighter-rouge">db</code>, <code class="language-plaintext highlighter-rouge">dw</code>, <code class="language-plaintext highlighter-rouge">dd</code>, <code class="language-plaintext highlighter-rouge">dq</code></strong> 같은 지시어로 변수를 메모리에 선언한다.</li>
  <li><strong>리틀엔디안</strong>은 작은 바이트부터 저장하는 방식, <strong>빅엔디안</strong>은 큰 바이트부터 저장하는 방식이다.</li>
  <li>Windows PC는 기본적으로 <strong>리틀엔디안</strong>이다.</li>
  <li>스택에 저장할 때도 리틀엔디안이 적용돼서, push한 값도 거꾸로 쌓인다.</li>
  <li>C언어나 어셈블리 디버깅할 때 리틀엔디안/빅엔디안 이해 못하면 메모리 값 해석을 절대 못한다.</li>
</ul>

<p><br />
다음 편 :<br />
assembly <strong>기본 명령어부터 익히기</strong></p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="assembly" /><category term="nasm" /><category term="assembly" /><summary type="html"><![CDATA[assembly #2]]></summary></entry><entry><title type="html">Assembly 기본 명령어부터 익히기-1</title><link href="https://ruyshy.github.io/assembly/assembly3-basic-commands-1/" rel="alternate" type="text/html" title="Assembly 기본 명령어부터 익히기-1" /><published>2025-04-22T00:00:00+09:00</published><updated>2025-04-22T00:00:00+09:00</updated><id>https://ruyshy.github.io/assembly/assembly3-basic-commands-1</id><content type="html" xml:base="https://ruyshy.github.io/assembly/assembly3-basic-commands-1/"><![CDATA[<p>assembly #3</p>

<h2 id="1-기본-명령어-리스트-windows-64bit-기준">1. 기본 명령어 리스트 (Windows 64bit 기준)</h2>

<table>
  <thead>
    <tr>
      <th>명령어</th>
      <th>역할</th>
      <th>비고</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>mov</strong></td>
      <td>데이터 복사</td>
      <td>레지스터, 메모리, 즉시값</td>
    </tr>
    <tr>
      <td><strong>add</strong></td>
      <td>덧셈</td>
      <td>레지스터끼리, 값 더하기</td>
    </tr>
    <tr>
      <td><strong>sub</strong></td>
      <td>뺄셈</td>
      <td>레지스터끼리, 값 빼기</td>
    </tr>
    <tr>
      <td><strong>inc</strong></td>
      <td>1 증가</td>
      <td>rax 같은 레지스터 1 증가</td>
    </tr>
    <tr>
      <td><strong>dec</strong></td>
      <td>1 감소</td>
      <td>rbx 같은 레지스터 1 감소</td>
    </tr>
    <tr>
      <td><strong>push</strong></td>
      <td>스택에 저장</td>
      <td>rsp를 줄이고 값 저장</td>
    </tr>
    <tr>
      <td><strong>pop</strong></td>
      <td>스택에서 꺼냄</td>
      <td>rsp를 늘리고 값 가져옴</td>
    </tr>
    <tr>
      <td><strong>call</strong></td>
      <td>함수 호출</td>
      <td>주소 저장 + 점프</td>
    </tr>
    <tr>
      <td><strong>ret</strong></td>
      <td>복귀</td>
      <td>스택에서 복귀 주소 꺼내서 점프</td>
    </tr>
    <tr>
      <td><strong>cmp</strong></td>
      <td>비교</td>
      <td>플래그만 세팅 (값은 안 바뀜)</td>
    </tr>
    <tr>
      <td><strong>jmp</strong></td>
      <td>무조건 점프</td>
      <td>그냥 이동</td>
    </tr>
    <tr>
      <td><strong>je, jne</strong></td>
      <td>조건부 점프</td>
      <td>Zero Flag 기반 (같으면/다르면)</td>
    </tr>
  </tbody>
</table>

<h2 id="2-각-명령어-설명">2. 각 명령어 설명</h2>

<h3 id="2-1-mov-데이터-복사">2-1. <strong>mov</strong> (데이터 복사)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">10</span>        <span class="c1">; rax에 10 저장</span>
<span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="nb">rax</span>       <span class="c1">; rax 값을 rbx로 복사</span>
</code></pre></div></div>

<p><strong>mov</strong>는 그냥 값을 복붙 한다고 생각하면 됌</p>

<h3 id="2-2-add-덧셈">2-2. <strong>add</strong> (덧셈)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>
<span class="nf">add</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">3</span>         <span class="c1">; rax = rax + 3</span>
</code></pre></div></div>

<p><strong>add dst, src</strong> ➔ dst = dst + src</p>

<h3 id="2-3-sub-뺄셈">2-3. <strong>sub</strong> (뺄셈)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>
<span class="nf">sub</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">2</span>         <span class="c1">; rax = rax - 2</span>
</code></pre></div></div>

<p><strong>sub dst, src</strong> ➔ dst = dst - src</p>

<h3 id="2-4-inc--dec-1-증가--1-감소">2-4. inc / dec (1 증가 / 1 감소)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rcx</span><span class="p">,</span> <span class="mi">10</span>
<span class="nf">inc</span> <span class="nb">rcx</span>            <span class="c1">; rcx = rcx + 1</span>
<span class="nf">dec</span> <span class="nb">rcx</span>            <span class="c1">; rcx = rcx - 1</span>
</code></pre></div></div>

<h3 id="2-5-push--pop-스택에-저장꺼내기">2-5. push / pop (스택에 저장/꺼내기)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">1234</span>
<span class="nf">push</span> <span class="nb">rax</span>           <span class="c1">; rax를 스택에 저장</span>
<span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">0</span>         <span class="c1">; rax를 0으로 초기화</span>
<span class="nf">pop</span> <span class="nb">rax</span>            <span class="c1">; 스택에서 rax 복원</span>
</code></pre></div></div>

<p>push하면 rsp 줄어들고,
pop하면 rsp 다시 올라간다.</p>

<h3 id="2-6-call--ret-함수-호출--복귀">2-6. call / ret (함수 호출 / 복귀)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">call</span> <span class="nv">my_function</span>   <span class="c1">; 함수 호출 (현재 위치를 스택에 저장하고 점프)</span>

<span class="c1">; ...</span>

<span class="nl">my_function:</span>
    <span class="c1">; (여기 코드 실행)</span>
    <span class="nf">ret</span>            <span class="c1">; 스택에서 복귀주소 꺼내서 돌아감</span>
</code></pre></div></div>

<p>함수 부르기/돌아오기 핵심. 나중에 스택 프레임이라는 중요한 것을 배울 예정.</p>

<h3 id="2-7-cmp--jmp--je--jne-비교-점프">2-7. cmp / jmp / je / jne (비교, 점프)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>
<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>         <span class="c1">; rax == 5 인지 비교 (ZF 플래그 세팅)</span>
<span class="no">je</span><span class="kd">  equ</span><span class="nb">al</span><span class="nv">_label</span>    <span class="c1">; 같으면 equal_label로 점프</span>
<span class="nf">jne</span> <span class="nv">not_equal_label</span><span class="c1">; 다르면 not_equal_label로 점프</span>
<span class="nf">jmp</span> <span class="nv">end_label</span>      <span class="c1">; 무조건 end_label로 점프</span>

<span class="nl">equal_label:</span>
    <span class="c1">; 여기는 rax == 5일 때 옴</span>
    <span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">not_equal_label:</span>
    <span class="c1">; 여기는 rax != 5일 때 옴</span>

<span class="nl">end_label:</span>
    <span class="c1">; 프로그램 계속 진행</span>
</code></pre></div></div>

<p>cmp는 값 자체를 안 바꿈! 플래그 레지스터만 조정한다. (ZF, SF, CF 등)</p>

<h3 id="2-8-기본-실습-문제1">2-8. 기본 실습 문제1</h3>
<pre><code class="language-assembly">    rax에 10 저장
	rbx에 20 저장
    rcx에 5 저장
    rax + rbx 결과를 rax에 저장
    rax - rcx 결과를 rax에 저장
    rax를 스택에 저장
    rbx를 스택에 저장
	스택에서 rbx 복구
	스택에서 rax 복구        
</code></pre>

<blockquote>
  <p>실습 문제1 풀기 전, 스택(Stack) 후입선출</p>
</blockquote>

<p>기본 코드 골격:</p>
<pre><code class="language-assembly">default rel
global main

section .text
main:

    ; 여기부터 코드 작성

.loop:
    jmp .loop

</code></pre>

<p>.loop:은 그냥 프로그램 끝에 멈추는 곳.</p>

<p><br /></p>
<details>
<summary>실습 문제 풀이1</summary>
<div>

    <pre><code class="language-assembly">default rel
global main

section .text
main:
    mov rax, 10        ; rax = 10
    mov rbx, 20        ; rbx = 20
    mov rcx, 5         ; rcx = 5

    add rax, rbx       ; rax = rax + rbx (10 + 20 = 30)
    sub rax, rcx       ; rax = rax - rcx (30 - 5 = 25)

    push rax           ; 스택에 rax(25) 저장
    push rbx           ; 스택에 rbx(20) 저장

    pop rbx            ; 스택에서 rbx 복구 (20)
    pop rax            ; 스택에서 rax 복구 (25)

.loop:
    jmp .loop          ; 프로그램 종료 대기
</code></pre>

    <p>동작 흐름 다시 정리
rax = 10</p>

    <p>rbx = 20</p>

    <p>rcx = 5</p>

    <p>add rax, rbx → rax = 30</p>

    <p>sub rax, rcx → rax = 25</p>

    <p>push rax → (25 스택 저장)</p>

    <p>push rbx → (20 스택 저장)</p>

    <p>pop rbx → (20 복구)</p>

    <p>pop rax → (25 복구)</p>

    <p>스택 동작 순서(후입선출 LIFO 구조)</p>

  </div>
</details>
<p><br /></p>

<h3 id="2-9-요약">2-9. 요약</h3>

<ul>
  <li>mov로 값 복사</li>
  <li>add/sub/inc/dec로 값 변경</li>
  <li>push/pop으로 스택 저장/복구</li>
  <li>call/ret으로 함수 호출/복귀</li>
  <li>cmp + je/jne/jmp로 조건 분기</li>
</ul>

<h2 id="3-기본-실습-문제2">3. 기본 실습 문제2</h2>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">; 1. rax에 10 저장</span>
<span class="c1">; 2. rbx에 20 저장</span>
<span class="c1">; 3. rax와 rbx를 더해서 rax에 저장</span>
<span class="c1">; 4. rax 값을 스택에 push</span>
<span class="c1">; 5. rax를 0으로 만든 뒤</span>
<span class="c1">; 6. 스택에서 rax 값을 pop해서 복구</span>
<span class="c1">; 7. rax == 30 이면 끝나는 루프</span>
</code></pre></div></div>

<h3 id="3-1-기본-실습-문제-풀기-전-알아두기">3-1. 기본 실습 문제 풀기 전 알아두기</h3>

<ol>
  <li>
    <p>기본 실습 문제 풀기 기본 환경 코드:</p>

    <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nf">default</span> <span class="nv">rel</span>
 <span class="nf">global</span> <span class="nv">main</span>
    
 <span class="nf">section</span> <span class="nv">.text</span>
 <span class="nl">main:</span>
     <span class="c1">; 여기에 코드 작성</span>
 <span class="nl">.end:</span>
     <span class="nf">jmp</span> <span class="nv">.end</span>   <span class="c1">; 무한 루프 (프로그램 강제 정지용)</span>
</code></pre></div>    </div>
  </li>
  <li>Assembly에서 “루프(반복)” 만들기
    <ol>
      <li>Assembly에서는 루프라는 걸 따로 지원하지 않는다.</li>
      <li><strong>“명령어 점프(jmp)” + “조건 비교(cmp + je/jne 등)”</strong> ➔ 이걸 조합해서 <strong>수동으로 루프를 만든다.</strong></li>
    </ol>
  </li>
  <li>루프 기본 패턴
    <ol>
      <li>
        <p>무한 루프 (종료 조건 없음)</p>

        <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nl">.loop:</span>
     <span class="c1">; (여기에 반복할 코드)</span>
     <span class="nf">jmp</span> <span class="nv">.loop</span>
</code></pre></div>        </div>

        <ol>
          <li><code class="language-plaintext highlighter-rouge">.loop:</code> 이라는 레이블(이름표)을 붙이고</li>
          <li>다시 <code class="language-plaintext highlighter-rouge">.loop</code>로 <code class="language-plaintext highlighter-rouge">jmp</code>해서 영원히 돌아오는 구조</li>
          <li>C로 치면 <code class="language-plaintext highlighter-rouge">while (1) { }</code> 와 같음.</li>
        </ol>
      </li>
    </ol>
  </li>
  <li>
    <p>카운터 기반 루프 (조건부 반복)</p>

    <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nf">mov</span> <span class="nb">rcx</span><span class="p">,</span> <span class="mi">5</span>        <span class="c1">; 반복할 횟수 5회</span>
    
 <span class="nl">.loop:</span>
     <span class="c1">; (여기에 반복할 코드)</span>
    
     <span class="nf">dec</span> <span class="nb">rcx</span>        <span class="c1">; rcx = rcx - 1</span>
     <span class="nf">cmp</span> <span class="nb">rcx</span><span class="p">,</span> <span class="mi">0</span>
     <span class="nf">jne</span> <span class="nv">.loop</span>      <span class="c1">; rcx != 0 이면 다시 반복</span>
</code></pre></div>    </div>

    <ol>
      <li><code class="language-plaintext highlighter-rouge">rcx</code>에 반복 횟수를 세팅하고</li>
      <li><code class="language-plaintext highlighter-rouge">dec</code>로 하나 줄이고</li>
      <li><code class="language-plaintext highlighter-rouge">cmp</code>로 0이 됐는지 비교</li>
      <li><code class="language-plaintext highlighter-rouge">jne</code>로 0이 아니면 다시 <code class="language-plaintext highlighter-rouge">.loop</code>로 점프</li>
      <li>
        <p>C로 치면:</p>

        <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nf">int</span> <span class="nv">i</span> <span class="err">=</span> <span class="mi">5</span><span class="c1">;</span>
 <span class="nf">while</span> <span class="p">(</span><span class="nv">i</span><span class="o">--</span><span class="p">)</span> <span class="err">{</span>
     <span class="o">//</span> <span class="err">반복</span>
 <span class="err">}</span>
</code></pre></div>        </div>
      </li>
    </ol>
  </li>
  <li>
    <p>루프 만들 때 필요한 명령어</p>

    <table>
      <thead>
        <tr>
          <th>명령어</th>
          <th>역할</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>jmp label</strong></td>
          <td>무조건 점프</td>
        </tr>
        <tr>
          <td><strong>cmp dst, src</strong></td>
          <td>비교 후 플래그 설정</td>
        </tr>
        <tr>
          <td><strong>je label</strong></td>
          <td>같으면 점프 (ZF = 1)</td>
        </tr>
        <tr>
          <td><strong>jne label</strong></td>
          <td>다르면 점프 (ZF = 0)</td>
        </tr>
        <tr>
          <td><strong>dec reg</strong></td>
          <td>1 감소</td>
        </tr>
      </tbody>
    </table>
  </li>
</ol>

<h3 id="3-2-문제-풀이">3-2. 문제 풀이</h3>

<p>먼저 3-1-a 에 기본 코드를 토대로 기본 실습 문제를 진행 해보자.</p>

<p>진행 후, 아래에 최종 답안과 풀이 과정을 확인 해보자.</p>

<details>
<summary>한 줄씩 풀이 보기</summary>
<div>

    <ol>
      <li><code class="language-plaintext highlighter-rouge">mov rax, 10</code>   ;mov 명렁어로 rax 에 10을 저장</li>
      <li><code class="language-plaintext highlighter-rouge">mov rbx, 20</code>   ;mov 명령어로 rbx 에 20을 저장</li>
      <li><code class="language-plaintext highlighter-rouge">add rax, rbx</code>   ;add 명령어로 rax 와 rbx 덧셈 연산 후, rax에 값을 저장</li>
      <li><code class="language-plaintext highlighter-rouge">push rax</code>   ; rax 값을 push = stack에 rax 값을 저장</li>
      <li><code class="language-plaintext highlighter-rouge">mov rax 0</code>   ; rax 값에 0을 저장</li>
      <li><code class="language-plaintext highlighter-rouge">pop rax</code>   ; stack 에서 이전에 저장한 rax(30)을 rax로 복구 = stack에서 rax값을 넣기</li>
      <li><code class="language-plaintext highlighter-rouge">cmp rax, 30</code>   ;rax값 과 30을 비교</li>
      <li><code class="language-plaintext highlighter-rouge">je .end</code>   ;<code class="language-plaintext highlighter-rouge">cmp</code> ➔ Zero Flag(ZF) 세팅, <code class="language-plaintext highlighter-rouge">je</code> ➔ ZF가 1이면 점프 (== 같을 때 점프)</li>
      <li><code class="language-plaintext highlighter-rouge">.loop:</code>   ;루프문</li>
      <li><code class="language-plaintext highlighter-rouge">jmp .loop</code>   ;(틀릴 경우 무한 루프)</li>
      <li><code class="language-plaintext highlighter-rouge">.end:</code>  ; <code class="language-plaintext highlighter-rouge">je .end</code> 에 선언한 곳</li>
      <li><code class="language-plaintext highlighter-rouge">jmp .end</code>  ;정상 종료 무한 루프 종료</li>
    </ol>

  </div>
</details>

<details>
<summary>최종 답안 보기</summary>
<div>

    <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">default</span> <span class="nv">rel</span>
<span class="nf">global</span> <span class="nv">main</span>

<span class="nf">section</span> <span class="nv">.text</span>
<span class="nl">main:</span>
    <span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">10</span>        <span class="c1">; 1. rax = 10</span>
    <span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="mi">20</span>        <span class="c1">; 2. rbx = 20</span>
    <span class="nf">add</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>       <span class="c1">; 3. rax = rax + rbx</span>
    <span class="nf">push</span> <span class="nb">rax</span>           <span class="c1">; 4. 스택에 저장</span>
    <span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">0</span>         <span class="c1">; 5. rax 초기화</span>
    <span class="nf">pop</span> <span class="nb">rax</span>            <span class="c1">; 6. 스택에서 복구</span>
    <span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">30</span>        <span class="c1">; 7. 30과 비교</span>
    <span class="nf">je</span> <span class="nv">.end</span>            <span class="c1">; 같으면 끝</span>
<span class="nl">.loop:</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>          <span class="c1">; (틀릴 경우 무한루프)</span>

<span class="nl">.end:</span>
    <span class="nf">jmp</span> <span class="nv">.end</span>           <span class="c1">; 정상 종료 무한루프</span>

</code></pre></div>    </div>

  </div>
</details>

<h3 id="3-2-체크포인트">3-2. 체크포인트</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">pop</code>, <code class="language-plaintext highlighter-rouge">cmp</code>, <code class="language-plaintext highlighter-rouge">je</code>, <code class="language-plaintext highlighter-rouge">jmp</code> 명령어 사용</li>
  <li>스택 쓰고 복구하는 흐름 익힘</li>
  <li>cmp/je 조건 분기 맛보기</li>
</ul>

<h2 id="4-조건-분기-명령어">4. 조건 분기 명령어</h2>

<p><strong>조건 분기 명령어란?</strong></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">cmp</code> 명령어로 <strong>두 값을 비교</strong>한 다음</li>
  <li>플래그 레지스터(<code class="language-plaintext highlighter-rouge">ZF</code>, <code class="language-plaintext highlighter-rouge">SF</code>, <code class="language-plaintext highlighter-rouge">CF</code>, <code class="language-plaintext highlighter-rouge">OF</code> 등)에 결과가 저장.</li>
  <li>이 플래그를 보고 <strong>조건에 맞으면 점프(jmp)</strong> 하는 구조.</li>
</ul>

<p><strong>조건 분기 명령어 핵심 3단계</strong></p>

<ul>
  <li><strong>cmp dst, src</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">dst - src</code>를 “계산만” 하고</li>
      <li><strong>값은 버리고 플래그만 세팅</strong>한다.</li>
    </ul>
  </li>
  <li><strong>플래그 레지스터 값 설정</strong>
    <ul>
      <li>Zero Flag(ZF), Sign Flag(SF), Carry Flag(CF), Overflow Flag(OF) 설정</li>
    </ul>
  </li>
  <li><strong>je, jne, jg, jl 등 분기 명령어로 점프</strong></li>
</ul>

<p>자주 쓰는 조건 분기 명령어</p>

<table>
  <thead>
    <tr>
      <th>명령어</th>
      <th>의미</th>
      <th>조건</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>je</strong></td>
      <td>Jump if Equal</td>
      <td>ZF == 1 (같으면)</td>
    </tr>
    <tr>
      <td><strong>jne</strong></td>
      <td>Jump if Not Equal</td>
      <td>ZF == 0 (다르면)</td>
    </tr>
    <tr>
      <td><strong>jg</strong></td>
      <td>Jump if Greater</td>
      <td>(SF == OF) &amp;&amp; (ZF == 0) (크면)</td>
    </tr>
    <tr>
      <td><strong>jge</strong></td>
      <td>Jump if Greater or Equal</td>
      <td>SF == OF (크거나 같으면)</td>
    </tr>
    <tr>
      <td><strong>jl</strong></td>
      <td>Jump if Less</td>
      <td>SF != OF (작으면)</td>
    </tr>
    <tr>
      <td><strong>jle</strong></td>
      <td>Jump if Less or Equal</td>
      <td>(SF != OF)</td>
    </tr>
  </tbody>
</table>

<p>Assembly에서는 거의 <strong>je, jne, jg, jl</strong>만 쓴다고 보면 됌.</p>

<h3 id="4-1-예제-1-je-같으면-점프">4-1. 예제 1: je (같으면 점프)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">10</span>
<span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="mi">10</span>
<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>    <span class="c1">; rax == rbx 비교</span>
<span class="no">je</span><span class="kd">  equ</span><span class="nb">al</span><span class="nv">_label</span> <span class="c1">; 같으면 equal_label로 점프</span>
<span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">equal_label:</span>
    <span class="c1">; 여기 도착</span>
    <span class="c1">; rax == rbx 일 때만</span>

<span class="nl">end_label:</span>
    <span class="c1">; 프로그램 종료</span>
</code></pre></div></div>

<p>rax == rbx이면 <code class="language-plaintext highlighter-rouge">equal_label</code>로 점프!</p>

<h3 id="4-2-예제-2-jne-다르면-점프">4-2. 예제 2: jne (다르면 점프)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">10</span>
<span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="mi">5</span>
<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>
<span class="nf">jne</span> <span class="nv">not_equal_label</span>
<span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">not_equal_label:</span>
    <span class="c1">; rax != rbx일 때 오는 곳</span>

<span class="nl">end_label:</span>
    <span class="c1">; 프로그램 종료</span>
</code></pre></div></div>

<p>rax != rbx이면 <code class="language-plaintext highlighter-rouge">not_equal_label</code>로 점프!</p>

<h3 id="4-3-예제-3-jg--jl-크거나-작을-때-점프">4-3. 예제 3: jg / jl (크거나 작을 때 점프)</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">20</span>
<span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="mi">10</span>
<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>
<span class="nf">jg</span> <span class="nv">greater_label</span>
<span class="nf">jl</span> <span class="nv">less_label</span>
<span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">greater_label:</span>
    <span class="c1">; rax &gt; rbx</span>
    <span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">less_label:</span>
    <span class="c1">; rax &lt; rbx</span>
    <span class="nf">jmp</span> <span class="nv">end_label</span>

<span class="nl">end_label:</span>
    <span class="c1">; 프로그램 종료</span>
</code></pre></div></div>

<p>rax &gt; rbx이면 <code class="language-plaintext highlighter-rouge">greater_label</code>,</p>

<p>rax &lt; rbx이면 <code class="language-plaintext highlighter-rouge">less_label</code> 로 간다.</p>

<h3 id="4-4-cmp가-뭘-하는지-한-번-더-강조">4-4. cmp가 뭘 하는지 한 번 더 강조</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>
</code></pre></div></div>

<p>→ 실제로는 <code class="language-plaintext highlighter-rouge">rax - rbx</code>를 계산</p>

<p>→ 그 결과에 따라 <strong>Zero/Sign/Carry/Overflow</strong> 플래그를 조정</p>

<p>→ 그리고 분기 명령어(<code class="language-plaintext highlighter-rouge">je</code>, <code class="language-plaintext highlighter-rouge">jg</code>, <code class="language-plaintext highlighter-rouge">jl</code> 등)가 이 플래그를 보는 거야.</p>

<blockquote>
  <p>즉, cmp는 “비교만 하고 아무것도 저장하지 않는다.”</p>

  <p><strong>플래그 세팅만 한다.</strong></p>

</blockquote>

<h3 id="4-5-요약">4-5. 요약</h3>

<table>
  <thead>
    <tr>
      <th>플래그</th>
      <th>의미</th>
      <th>영향 받는 분기</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ZF (Zero Flag)</strong></td>
      <td>두 값이 같으면 세팅</td>
      <td>je, jne</td>
    </tr>
    <tr>
      <td><strong>SF (Sign Flag)</strong></td>
      <td>결과가 음수면 세팅</td>
      <td>jg, jl</td>
    </tr>
    <tr>
      <td><strong>OF (Overflow Flag)</strong></td>
      <td>오버플로우 발생 여부</td>
      <td>jg, jl</td>
    </tr>
    <tr>
      <td><strong>CF (Carry Flag)</strong></td>
      <td>자리 올림/내림 발생 여부</td>
      <td>(unsigned 비교에 사용)</td>
    </tr>
  </tbody>
</table>

<p>signed/unsigned 구분이 있는데, 일단 지금은 <strong>정수 부호 있는(signed) 비교</strong>만 다룬다.</p>

<h3 id="4-6-미션">4-6. 미션</h3>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">; 1. rax에 7 저장</span>
<span class="c1">; 2. rbx에 5 저장</span>
<span class="c1">; 3. cmp로 비교</span>
<span class="c1">; 4. 크면 "greater", 작으면 "less" 레이블로 점프</span>
<span class="c1">; 5. 둘 다 아니면 "equal" 레이블로 점프</span>
</code></pre></div></div>

<p>기본 코드:</p>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">default</span> <span class="nv">rel</span>
<span class="nf">global</span> <span class="nv">main</span>

<span class="nf">section</span> <span class="nv">.text</span>
<span class="nl">main:</span>
    <span class="c1">; 여기부터 네 코드</span>

<span class="nl">.loop:</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>          <span class="c1">; 프로그램 끝 (무한 대기)</span>

<span class="nl">greater:</span>
    <span class="c1">; 여기는 rax &gt; rbx 일 때 오는 곳</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>

<span class="nl">less:</span>
    <span class="c1">; 여기는 rax &lt; rbx 일 때 오는 곳</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>

<span class="nl">equal:</span>
    <span class="c1">; 여기는 rax == rbx 일 때 오는 곳</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>
</code></pre></div></div>

<details>
<summary>미션 풀이:</summary>
<div>

    <div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">default</span> <span class="nv">rel</span>
<span class="nf">global</span> <span class="nv">main</span>

<span class="nf">section</span> <span class="nv">.text</span>
<span class="nl">main:</span>
    <span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">7</span>         <span class="c1">; rax = 7</span>
    <span class="nf">mov</span> <span class="nb">rbx</span><span class="p">,</span> <span class="mi">5</span>         <span class="c1">; rbx = 5</span>
    <span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="nb">rbx</span>       <span class="c1">; rax - rbx 비교 (7 - 5)</span>
    <span class="nf">jg</span> <span class="nv">greater</span>         <span class="c1">; rax &gt; rbx 이면 greater로 점프</span>
    <span class="nf">jl</span> <span class="nv">less</span>            <span class="c1">; rax &lt; rbx 이면 less로 점프</span>
    <span class="no">je</span><span class="kd"> equ</span><span class="nb">al</span>           <span class="c1">; rax == rbx 이면 equal로 점프</span>

<span class="nl">.loop:</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>          <span class="c1">; 프로그램 끝 (무한 대기)</span>

<span class="nl">greater:</span>
    <span class="c1">; rax &gt; rbx일 때</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>

<span class="nl">less:</span>
    <span class="c1">; rax &lt; rbx일 때</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>

<span class="nl">equal:</span>
    <span class="c1">; rax == rbx일 때</span>
    <span class="nf">jmp</span> <span class="nv">.loop</span>
</code></pre></div>    </div>

    <p><strong>흐름 해석</strong></p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">7</code>과 <code class="language-plaintext highlighter-rouge">5</code> 비교 → 결과는 “7 &gt; 5”니까 <code class="language-plaintext highlighter-rouge">jg greater</code> 실행</li>
      <li>프로그램은 <code class="language-plaintext highlighter-rouge">greater:</code>로 점프</li>
      <li>거기서 <code class="language-plaintext highlighter-rouge">.loop</code>로 넘어가서 무한 대기</li>
    </ul>

  </div>
</details>
<p><br /></p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="assembly" /><category term="nasm" /><category term="assembly" /><summary type="html"><![CDATA[assembly #3]]></summary></entry><entry><title type="html">GitHub Blog 검색엔진 설정</title><link href="https://ruyshy.github.io/blog/blog11-search-engine/" rel="alternate" type="text/html" title="GitHub Blog 검색엔진 설정" /><published>2025-04-22T00:00:00+09:00</published><updated>2025-04-22T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog11-search-engine</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog11-search-engine/"><![CDATA[<p>GitHub Blog #11</p>

<h2 id="검색-엔진-개요">검색 엔진 개요</h2>

<p>월드 와이드 웹 상에 존재하는 정보와 웹 사이트를 검색하기 위한 프로그램. 웹의 정보를 긁어오는 소프트웨어는 크롤러라고 부르고, 그 행위는 크롤링이라고 부른다. 참고로 일반 검색 엔진에 안 잡히는 웹을 딥 웹이라고 한다.</p>

<h2 id="검색-엔진-종류">검색 엔진 종류</h2>

<p>구글, 네이버, Bing, zum, 네이트, 다음… 여러가지가 있습니다.</p>

<h2 id="google-search-console">Google Search Console</h2>

<p><strong>Google Search Console :</strong> <a href="https://search.google.com/search-console/welcome?utm_source=about-page">https://search.google.com/search-console/welcome?utm_source=about-page</a></p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image1.png" alt="image1.png" style="max-width:100%;" /></p>

<p>접속 후, URL 접두어에 자기 블로그 주소를 추가 후, 계숙을 눌러줍니다.</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image2.png" alt="image2.png" style="max-width:100%;" /></p>

<p>계숙 버튼을 눌러 진행을 하면 다음 화면이 나오는데, 파일을 다운로드 받고 다운 받은 파일을 깃허브 저장소 최상단 폴더에 넣어줍니다.</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image3.png" alt="image3.png" style="max-width:100%;" /></p>

<p>저장소에 push를 진행하고, 배포가 완료된 후, 확인 버튼을 눌러 진행하면 다음과 같이 창이 바뀌는데, 속성으로 이동을 눌러 설정을 해주도록 합시다.</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image4.png" alt="image4.png" style="max-width:100%;" /></p>

<p>Sitemap을 눌러 해당 설정 화면이 나오면 sitemap.xml을 추가해주는 작업을 진행해야 합니다. 저는 예전에 설정해둔 sitemap이 제출이 되어있긴하네요. 일단 진행해봅시다.</p>

<h2 id="sitemapxml-생성">\sitemap.xml 생성</h2>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: null
---

<span class="cp">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span>
<span class="nt">&lt;urlset</span> <span class="na">xmlns:xsi=</span><span class="s">"http://www.w3.org/2001/XMLSchema-instance"</span>
  <span class="na">xsi:schemaLocation=</span><span class="s">"http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd"</span>
  <span class="na">xmlns=</span><span class="s">"http://www.sitemaps.org/schemas/sitemap/0.9"</span><span class="nt">&gt;</span>
  {% for post in site.posts %}
    <span class="nt">&lt;url&gt;</span>
      <span class="nt">&lt;loc&gt;</span>{{ site.url }}{{ post.url }}<span class="nt">&lt;/loc&gt;</span>
      {% if post.last_modified_at == null %}
        <span class="nt">&lt;lastmod&gt;</span>{{ post.date | date_to_xmlschema }}<span class="nt">&lt;/lastmod&gt;</span>
      {% else %}
        <span class="nt">&lt;lastmod&gt;</span>{{ post.last_modified_at | date_to_xmlschema }}<span class="nt">&lt;/lastmod&gt;</span>
      {% endif %}

      {% if post.sitemap.changefreq == null %}
        <span class="nt">&lt;changefreq&gt;</span>weekly<span class="nt">&lt;/changefreq&gt;</span>
      {% else %}
        <span class="nt">&lt;changefreq&gt;</span>{{ post.sitemap.changefreq }}<span class="nt">&lt;/changefreq&gt;</span>
      {% endif %}

      {% if post.sitemap.priority == null %}
          <span class="nt">&lt;priority&gt;</span>0.5<span class="nt">&lt;/priority&gt;</span>
      {% else %}
        <span class="nt">&lt;priority&gt;</span>{{ post.sitemap.priority }}<span class="nt">&lt;/priority&gt;</span>
      {% endif %}

    <span class="nt">&lt;/url&gt;</span>
  {% endfor %}
  
<span class="nt">&lt;/urlset&gt;</span>
</code></pre></div></div>

<p>작성 후, 깃허브 저장소에 push를 진행합니다.</p>

<p>깃허브 action으로 배포가 진행이 되었다면 새 사이트맵 제출을 해줍니다.</p>

<p>(sitemap.xml)</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image5.png" alt="image5.png" style="max-width:100%;" /></p>

<p>일단 성공적으로 제출이 진행 되었네요.</p>

<h2 id="robotstxt-생성">\robots.txt 생성</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User-agent: *
Allow: /
Sitemap: https://ruyshy.github.io/sitemap.xml
</code></pre></div></div>

<p>robots.txt 생성 후, 깃허브 저장소에 push, 다시 사이트 맵을 제출해줍니다.</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image6.png" alt="image6.png" style="max-width:100%;" /></p>

<p>구글에 검색해보니 잘 보이는 듯 합니다.</p>

<h2 id="추가-소유권-인증-방법2">추가 소유권 인증 방법2</h2>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image7.png" alt="image7.png" style="max-width:100%;" /></p>

<p>설정 → 소유권 인증 으로 들어가보면 HTML 태그가 있습니다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"google-site-verification"</span> <span class="na">content=</span><span class="s">"0kzzhmbnk0AQfutCd0jcHHfNYMmXzafnXZVCY-fMSOU"</span> <span class="nt">/&gt;</span>
</code></pre></div></div>

<p>content 안에 있는 내용을 복사한 뒤, _config.yml 에서 <code class="language-plaintext highlighter-rouge">google-site-verification:</code> 해당 부분을 찾습니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">google_site_verification </span><span class="pi">:</span> <span class="s">0kzzhmbnk0AQfutCd0jcHHfNYMmXzafnXZVCY-fMSOU</span>
</code></pre></div></div>

<p>위와 같이 content에 있는 내용을 추가해줍니다. 추가 후, 깃허브 저장소에 push 진행 → 확인을 눌러 진행.</p>

<p><img src="/assets/images/blog/2025-04-22-blog11-search-engine/image8.png" alt="image8.png" style="max-width:100%;" /></p>

<p>인증이 완료되었네요! 굿굿</p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #11]]></summary></entry><entry><title type="html">GitHub Blog 코드블럭 copy 버튼</title><link href="https://ruyshy.github.io/blog/blog10-code-block-copy-button/" rel="alternate" type="text/html" title="GitHub Blog 코드블럭 copy 버튼" /><published>2025-04-21T00:00:00+09:00</published><updated>2025-04-21T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog10-code-block-copy-button</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog10-code-block-copy-button/"><![CDATA[<p>GitHub Blog #10</p>

<h2 id="code-block-copy-button-추가">code block copy button 추가</h2>

<h3 id="includecode-block_customhtml-추가">\include\code-block_custom.html 추가</h3>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;script </span><span class="na">src=</span><span class="s">"https://cdnjs.cloudflare.com/ajax/libs/clipboard.js/2.0.11/clipboard.min.js"</span><span class="nt">&gt;&lt;/script&gt;</span>
<span class="nt">&lt;script&gt;</span>
<span class="nb">document</span><span class="p">.</span><span class="nf">addEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">DOMContentLoaded</span><span class="dl">'</span><span class="p">,</span> <span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
  <span class="nb">document</span><span class="p">.</span><span class="nf">querySelectorAll</span><span class="p">(</span><span class="dl">'</span><span class="s1">div.highlighter-rouge</span><span class="dl">'</span><span class="p">).</span><span class="nf">forEach</span><span class="p">(</span><span class="kd">function</span><span class="p">(</span><span class="nx">block</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="nx">button</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">createElement</span><span class="p">(</span><span class="dl">'</span><span class="s1">button</span><span class="dl">'</span><span class="p">);</span>
    <span class="nx">button</span><span class="p">.</span><span class="nx">className</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">copy-button</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">button</span><span class="p">.</span><span class="nx">textContent</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">Copy</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">button</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">position</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">absolute</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">button</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">top</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">0.25em</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">button</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">right</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">0.25em</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">block</span><span class="p">.</span><span class="nx">style</span><span class="p">.</span><span class="nx">position</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">relative</span><span class="dl">'</span><span class="p">;</span>
    <span class="nx">block</span><span class="p">.</span><span class="nf">appendChild</span><span class="p">(</span><span class="nx">button</span><span class="p">);</span>
  <span class="p">});</span>

  <span class="kd">var</span> <span class="nx">clipboard</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClipboardJS</span><span class="p">(</span><span class="dl">'</span><span class="s1">.copy-button</span><span class="dl">'</span><span class="p">,</span> <span class="p">{</span>
    <span class="na">target</span><span class="p">:</span> <span class="kd">function</span><span class="p">(</span><span class="nx">trigger</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nx">trigger</span><span class="p">.</span><span class="nx">parentElement</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">pre</span><span class="dl">'</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">});</span>

  <span class="nx">clipboard</span><span class="p">.</span><span class="nf">on</span><span class="p">(</span><span class="dl">'</span><span class="s1">success</span><span class="dl">'</span><span class="p">,</span> <span class="kd">function</span><span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="nx">originalText</span> <span class="o">=</span> <span class="nx">e</span><span class="p">.</span><span class="nx">trigger</span><span class="p">.</span><span class="nx">textContent</span><span class="p">;</span>
    <span class="nx">e</span><span class="p">.</span><span class="nx">trigger</span><span class="p">.</span><span class="nx">textContent</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">Copied!</span><span class="dl">'</span><span class="p">;</span>
    <span class="nf">setTimeout</span><span class="p">(</span><span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
      <span class="nx">e</span><span class="p">.</span><span class="nx">trigger</span><span class="p">.</span><span class="nx">textContent</span> <span class="o">=</span> <span class="nx">originalText</span><span class="p">;</span>
    <span class="p">},</span> <span class="mi">1500</span><span class="p">);</span>
    <span class="nx">e</span><span class="p">.</span><span class="nf">clearSelection</span><span class="p">();</span>
  <span class="p">});</span>
<span class="p">});</span>
<span class="nt">&lt;/script&gt;</span>
</code></pre></div></div>

<ul>
  <li>복사 성공하면 버튼이 1.5초 동안 <code class="language-plaintext highlighter-rouge">"Copied!"</code>로 변하고</li>
  <li>다시 <code class="language-plaintext highlighter-rouge">"Copy"</code>로 자동 복구됨.</li>
  <li>오류나면 콘솔에 에러도 출력되게 함.</li>
  <li>local로 하고 싶을 때, https://cdnjs.cloudflare.com/ajax/libs/clipboard.js/2.0.11/clipboard.min.js 해당 파일을 Ctrl+S로 저장 후, /assets/js/vendor/ 해당 경로에 clipboard.min.js 옮긴 후, 맨 위 줄 아래처럼 변경하면 local로 진행 가능!</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;script src="/assets/js/vendor/clipboard.min.js"&gt;&lt;/script&gt;</code></li>
</ul>

<h3 id="_layoutsdefaulthtml-코드에-추가">_layouts\default.html 코드에 추가</h3>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
---

<span class="cp">&lt;!doctype html&gt;</span>

{% include copyright.html %}
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"{{ site.locale | replace: "</span><span class="na">_</span><span class="err">",</span> <span class="err">"</span><span class="na">-</span><span class="err">"</span> <span class="err">|</span> <span class="na">default:</span> <span class="err">"</span><span class="na">en</span><span class="err">"</span> <span class="err">}}"</span> <span class="na">class=</span><span class="s">"no-js"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    {% include head.html %}
    {% include head/custom.html %}
  <span class="nt">&lt;/head&gt;</span>

  <span class="nt">&lt;body</span> <span class="na">class=</span><span class="s">"layout--{{ page.layout | default: layout.layout }}{% if page.classes or layout.classes %}{{ page.classes | default: layout.classes | join: ' ' | prepend: ' ' }}{% endif %}"</span> <span class="na">dir=</span><span class="s">"{% if site.rtl %}rtl{% else %}ltr{% endif %}"</span><span class="nt">&gt;</span>
    {% include_cached skip-links.html %}
    {% include_cached masthead.html %}

    <span class="nt">&lt;div</span> <span class="na">class=</span><span class="s">"initial-content"</span><span class="nt">&gt;</span>
      {{ content }}
      {% include after-content.html %}
    <span class="nt">&lt;/div&gt;</span>

    {% if site.search == true %}
      <span class="nt">&lt;div</span> <span class="na">class=</span><span class="s">"search-content"</span><span class="nt">&gt;</span>
        {% include_cached search/search_form.html %}
      <span class="nt">&lt;/div&gt;</span>
    {% endif %}

    <span class="nt">&lt;div</span> <span class="na">id=</span><span class="s">"footer"</span> <span class="na">class=</span><span class="s">"page__footer"</span><span class="nt">&gt;</span>
      <span class="nt">&lt;footer&gt;</span>
        {% include footer/custom.html %}
        {% include_cached footer.html %}
      <span class="nt">&lt;/footer&gt;</span>
    <span class="nt">&lt;/div&gt;</span>
		<span class="c">&lt;!-- 여기 추가 --&gt;</span>
    {% include code-block_custom.html %}
    

    {% include scripts.html %}
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>

</code></pre></div></div>

<h3 id="_sassminimal-mistakes_buttonsscss-코드에-추가">_sass\minimal-mistakes_buttons.scss 코드에 추가</h3>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">.copy-button</span> <span class="p">{</span>
  <span class="nl">background</span><span class="p">:</span> <span class="mh">#444</span><span class="p">;</span>
  <span class="nl">color</span><span class="p">:</span> <span class="mh">#fff</span><span class="p">;</span>
  <span class="nl">border</span><span class="p">:</span> <span class="nb">none</span><span class="p">;</span>
  <span class="nl">padding</span><span class="p">:</span> <span class="m">0</span><span class="mi">.3em</span> <span class="m">0</span><span class="mi">.7em</span><span class="p">;</span>
  <span class="nl">font-size</span><span class="p">:</span> <span class="m">0</span><span class="mi">.7em</span><span class="p">;</span>
  <span class="nl">border-radius</span><span class="p">:</span> <span class="m">5px</span><span class="p">;</span>
  <span class="nl">cursor</span><span class="p">:</span> <span class="nb">pointer</span><span class="p">;</span>
  <span class="nl">opacity</span><span class="p">:</span> <span class="m">0</span><span class="mi">.8</span><span class="p">;</span>
  <span class="nl">transition</span><span class="p">:</span> <span class="n">all</span> <span class="m">0</span><span class="mi">.2s</span> <span class="n">ease</span><span class="p">;</span>
  <span class="nl">z-index</span><span class="p">:</span> <span class="m">2</span><span class="p">;</span>
<span class="p">}</span>

<span class="nc">.copy-button</span><span class="nd">:hover</span> <span class="p">{</span>
  <span class="nl">background</span><span class="p">:</span> <span class="mh">#666</span><span class="p">;</span>
  <span class="nl">opacity</span><span class="p">:</span> <span class="m">1</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div>

<p>버튼이 훨씬 부드럽고 모던하게 변함.</p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #10]]></summary></entry><entry><title type="html">Assembly 시작하기</title><link href="https://ruyshy.github.io/assembly/assembly1-start/" rel="alternate" type="text/html" title="Assembly 시작하기" /><published>2025-04-18T00:00:00+09:00</published><updated>2025-04-18T00:00:00+09:00</updated><id>https://ruyshy.github.io/assembly/assembly1-start</id><content type="html" xml:base="https://ruyshy.github.io/assembly/assembly1-start/"><![CDATA[<p>assembly #1</p>

<p>시작하기 앞 서, 학습할 사이트 목록 입니다.</p>

<p>드림 해커 : https://dreamhack.io/ </p>

<p>CrackMe : https://crackmy.app/</p>

<h1 id="어셈블리assembly란"><strong>어셈블리(Assembly)란?</strong></h1>

<p>어셈블리어는 사람이 읽을 수 있는 저수준 언어로, CPU가 실제로 이해하는 기계어(machine code)와 1:1로 대응됨. 쉽게 말하면, CPU와 직접 대화하기 직전 단계라고 볼 수 있음.</p>

<p>한 줄로 요약하면 “CPU가 이해할 수 있는 인간 최저 레벨 언어” (C보다 훨씬 더 하드웨어에 가깝다.)</p>

<h2 id="assembly에서-필수로-알아야-할-개념"><strong>Assembly에서 필수로 알아야 할 개념</strong></h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>레지스터(Register)</td>
      <td>CPU 안에 있는 초고속 저장소 (eax, ebx, ecx, edx 같은 것)</td>
    </tr>
    <tr>
      <td>스택(Stack)</td>
      <td>함수 호출할 때 사용하는 메모리 공간 (push/pop)</td>
    </tr>
    <tr>
      <td>메모리 주소(Address)</td>
      <td>직접 RAM의 위치를 다룬다</td>
    </tr>
    <tr>
      <td>명령어(Instruction)</td>
      <td><code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code>, <code class="language-plaintext highlighter-rouge">jmp</code> 같은 한 줄짜리 CPU 명령</td>
    </tr>
    <tr>
      <td>플래그(Flag)</td>
      <td>연산 결과를 저장하는 상태 비트 (Zero Flag, Carry Flag 등)</td>
    </tr>
  </tbody>
</table>

<h2 id="공부-순서-추천"><strong>공부 순서 추천</strong></h2>

<ul>
  <li>
    <p><strong>레지스터랑 메모리 구조 이해</strong></p>

    <p>(eax, esp, ebp 같은 거 감 잡기)</p>
  </li>
  <li>
    <p><strong>기본 명령어부터 익히기</strong></p>

    <p>(mov, add, sub, cmp, jmp, call, ret)</p>
  </li>
  <li>
    <p><strong>함수 호출 규칙(Call Convention)</strong></p>

    <p>(stdcall, cdecl 이런 거)</p>
  </li>
  <li>
    <p><strong>스택과 함수 호출 분석</strong></p>

    <p>(push, pop으로 스택 프레임 조작하는 것 보기)</p>
  </li>
  <li>
    <p><strong>조건 분기(Conditional Jump)</strong></p>

    <p>(cmp, je, jne, jg, jl 등 분기 흐름 익히기)</p>
  </li>
  <li>
    <p><strong>간단한 프로그램 작성</strong></p>

    <p>(Hello World 출력 Assembly 버전)</p>
  </li>
  <li>
    <p><strong>디버거로 어셈블리 흐름 읽는 연습</strong></p>

    <p>(x64dbg로 한 줄 한 줄 따라가며 이해)</p>
  </li>
</ul>

<h1 id="1-레지스터랑-메모리-구조-이해"><strong>1. 레지스터랑 메모리 구조 이해</strong></h1>

<h2 id="레지스터register란"><strong>레지스터(Register)란?</strong></h2>

<ul>
  <li>CPU 안에 있는 <strong>작고 빠른 기억장치</strong>.</li>
  <li><strong>변수 같은 거 저장</strong>하는 초고속 칸이라고 생각하면 됨.</li>
</ul>

<h2 id="레지스터의-전체-분류-x86-64-기준"><strong>레지스터의 전체 분류 (x86-64 기준)</strong></h2>

<table>
  <thead>
    <tr>
      <th>분류</th>
      <th>예시</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>일반 목적 레지스터 (GPR)</strong></td>
      <td>RAX, RBX, RCX, …</td>
      <td>연산, 데이터 저장, 함수 인자 전달</td>
    </tr>
    <tr>
      <td><strong>세그먼트 레지스터</strong></td>
      <td>CS, DS, SS, ES, FS, GS</td>
      <td>메모리 세그먼트 구분</td>
    </tr>
    <tr>
      <td><strong>플래그 레지스터 (FLAGS/RFLAGS)</strong></td>
      <td>EFLAGS, RFLAGS</td>
      <td>조건 분기, 연산 결과 상태 저장</td>
    </tr>
    <tr>
      <td><strong>포인터/인덱스 레지스터</strong></td>
      <td>RIP, RSP, RBP, RSI, RDI</td>
      <td>명령어 포인터, 스택, 베이스, 인덱싱</td>
    </tr>
    <tr>
      <td><strong>제어 레지스터 (CRx)</strong></td>
      <td>CR0, CR2, CR3, CR4</td>
      <td>페이지 테이블, 보호모드 등 제어</td>
    </tr>
    <tr>
      <td><strong>디버그 레지스터</strong></td>
      <td>DR0–DR7</td>
      <td>브레이크포인트 설정 등에 사용</td>
    </tr>
  </tbody>
</table>

<p>대부분 실무에서 GPR, FLAGS, 세그먼트 레지스터까지 알면 충분</p>

<h2 id="32bit-레지스터-x86-기준"><strong>32bit 레지스터 (x86 기준)</strong></h2>

<table>
  <thead>
    <tr>
      <th>레지스터</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>EAX</strong></td>
      <td>연산 결과 저장 (주로 사용)</td>
    </tr>
    <tr>
      <td><strong>EBX</strong></td>
      <td>데이터 베이스 역할 (서브 저장소)</td>
    </tr>
    <tr>
      <td><strong>ECX</strong></td>
      <td>루프 카운터 (반복문 돌릴 때)</td>
    </tr>
    <tr>
      <td><strong>EDX</strong></td>
      <td>입출력 연산 결과 저장 (EAX랑 비슷하지만 구분)</td>
    </tr>
    <tr>
      <td><strong>ESI</strong></td>
      <td>소스 인덱스 (메모리 복사 같은 작업할 때)</td>
    </tr>
    <tr>
      <td><strong>EDI</strong></td>
      <td>목적지 인덱스 (메모리 복사 목적지)</td>
    </tr>
    <tr>
      <td><strong>ESP</strong></td>
      <td>스택 포인터 (현재 스택 꼭대기 가리킴)</td>
    </tr>
    <tr>
      <td><strong>EBP</strong></td>
      <td>베이스 포인터 (함수 스택 프레임 관리)</td>
    </tr>
    <tr>
      <td><strong>EIP</strong></td>
      <td>다음 실행할 명령어 주소 (Instruction Pointer)</td>
    </tr>
  </tbody>
</table>

<p>참고: 64bit는 EAX → RAX 처럼 “R” 붙는다 (RAX, RBX, …)</p>

<h2 id="64bit-레지스터"><strong>64bit 레지스터</strong></h2>

<table>
  <thead>
    <tr>
      <th>레지스터</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>RAX</strong></td>
      <td>연산 결과 저장 (64bit)</td>
    </tr>
    <tr>
      <td><strong>RBX</strong></td>
      <td>데이터 저장용 (서브 저장소)</td>
    </tr>
    <tr>
      <td><strong>RCX</strong></td>
      <td>루프 카운터</td>
    </tr>
    <tr>
      <td><strong>RDX</strong></td>
      <td>입출력 결과 저장</td>
    </tr>
    <tr>
      <td><strong>RSI</strong></td>
      <td>소스 인덱스</td>
    </tr>
    <tr>
      <td><strong>RDI</strong></td>
      <td>목적지 인덱스</td>
    </tr>
    <tr>
      <td><strong>RSP</strong></td>
      <td>스택 포인터 (stack pointer)</td>
    </tr>
    <tr>
      <td><strong>RBP</strong></td>
      <td>베이스 포인터 (base pointer)</td>
    </tr>
    <tr>
      <td><strong>RIP</strong></td>
      <td>명령어 포인터 (instruction pointer)</td>
    </tr>
  </tbody>
</table>

<h2 id="세그먼트-레지스터-segment-registers"><strong>세그먼트 레지스터 (Segment Registers)</strong></h2>

<table>
  <thead>
    <tr>
      <th>레지스터</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>CS</strong></td>
      <td>Code Segment: 명령어(코드)가 들어있는 영역</td>
    </tr>
    <tr>
      <td><strong>DS</strong></td>
      <td>Data Segment: 일반 데이터가 들어있는 영역</td>
    </tr>
    <tr>
      <td><strong>SS</strong></td>
      <td>Stack Segment: 스택 데이터가 들어있는 영역</td>
    </tr>
    <tr>
      <td><strong>ES/FS/GS</strong></td>
      <td>확장 세그먼트: 특수 목적 (스레드 로컬 등)</td>
    </tr>
  </tbody>
</table>

<p>중요한 사실:</p>

<blockquote>
  <p>64bit에서는 대부분 세그먼트 의미가 무시되거나 고정됨. 예를 들어:</p>

  <ul>
    <li>CS, DS, SS 같은 건 거의 고정값으로 유지되고</li>
    <li>FS, GS만 스레드 로컬 데이터(TIB/TEB 접근)처럼 제한적으로 사용돼.</li>
  </ul>
</blockquote>

<p>즉, 64bit에서는 세그먼트 레지스터 사용 거의 안 한다. 예외적으로 FS/GS는 구조체 접근할 때 쓰기도 함 (예: gs:[0x30] → PEB)</p>

<h2 id="플래그-레지스터-rflags--eflags"><strong>플래그 레지스터 (RFLAGS / EFLAGS)</strong></h2>

<table>
  <thead>
    <tr>
      <th>플래그 이름</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ZF (Zero Flag)</strong></td>
      <td>연산 결과가 0이면 설정됨</td>
    </tr>
    <tr>
      <td><strong>CF (Carry Flag)</strong></td>
      <td>자리 올림 발생 시 설정됨</td>
    </tr>
    <tr>
      <td><strong>SF (Sign Flag)</strong></td>
      <td>부호 비트 (음수면 1)</td>
    </tr>
    <tr>
      <td><strong>OF (Overflow Flag)</strong></td>
      <td>부호 있는 오버플로우 발생 시 설정됨</td>
    </tr>
    <tr>
      <td><strong>PF (Parity Flag)</strong></td>
      <td>결과의 1 비트 개수가 짝수면 설정됨</td>
    </tr>
    <tr>
      <td><strong>AF (Auxiliary Carry)</strong></td>
      <td>BCD 연산 시 사용됨</td>
    </tr>
  </tbody>
</table>

<p>cmp, test 같은 명령어는 → 내부적으로 연산 후 플래그 레지스터를 변경하고 → je, jne, jg, jl 같은 조건 분기 명령어에서 → 플래그 값을 참조해서 분기한다.</p>

<p>예시:</p>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>
<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">5</span>     <span class="c1">; ZF = 1</span>
<span class="no">je</span><span class="kd">  equ</span><span class="nb">al</span><span class="nv">_label</span> <span class="c1">; ZF가 1이면 점프함 (==)</span>

<span class="nf">cmp</span> <span class="nb">rax</span><span class="p">,</span> <span class="mi">3</span>     <span class="c1">; ZF = 0, SF = 0</span>
<span class="nf">jg</span>  <span class="nv">greater_label</span> <span class="c1">; rax &gt; 3 → 점프</span>

</code></pre></div></div>

<p>플래그가 이렇게 연산 결과를 기억하고, 조건 분기를 가능하게 만들어주는 핵심 역할을 한다.</p>

<h2 id="레지스터-요약"><strong>레지스터 요약</strong></h2>

<table>
  <thead>
    <tr>
      <th>분류</th>
      <th>레지스터</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>일반 목적</strong></td>
      <td>RAX~R15</td>
      <td>데이터 저장/연산</td>
    </tr>
    <tr>
      <td><strong>세그먼트</strong></td>
      <td>CS, DS, SS, FS, GS</td>
      <td>메모리 세그먼트 (64bit에선 거의 무시됨)</td>
    </tr>
    <tr>
      <td><strong>플래그</strong></td>
      <td>RFLAGS</td>
      <td>연산 결과 상태 저장 (분기 판단용)</td>
    </tr>
  </tbody>
</table>

<h2 id="암기-포인트"><strong>암기 포인트</strong></h2>

<ul>
  <li><strong>64bit에서는 세그먼트 레지스터 거의 안 씀</strong></li>
  <li><strong>플래그 레지스터는 조건 분기에 핵심</strong></li>
  <li><strong>일반 레지스터만 쓰는 게 거의 대부분</strong> (RAX, RCX, RSI, …)</li>
</ul>

<h2 id="rax-eax-ax-ah-al-구조"><strong>RAX, EAX, AX, AH, AL 구조</strong></h2>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">RAX</span>  <span class="o">(</span><span class="nt">64bit</span><span class="o">)</span>
 <span class="err">└──</span> <span class="nt">EAX</span>  <span class="o">(</span><span class="err">하위</span> <span class="nt">32bit</span><span class="o">)</span>
      <span class="err">└──</span> <span class="nt">AX</span>   <span class="o">(</span><span class="err">하위</span> <span class="nt">16bit</span><span class="o">)</span>
           <span class="err">├──</span> <span class="nt">AH</span> <span class="o">(</span><span class="err">상위</span> <span class="nt">8bit</span><span class="o">)</span>
           <span class="err">└──</span> <span class="nt">AL</span> <span class="o">(</span><span class="err">하위</span> <span class="nt">8bit</span><span class="o">)</span>

</code></pre></div></div>

<p>32bit에서는 EAX, EBX, 64bit는 앞에 “R” 붙고 64bit 크기로 바뀐다. 추가로: <strong>r8 ~ r15</strong> 라는 새로운 레지스터들도 생겼음. → 64bit에서는 레지스터가 더 많음 (덕분에 더 자유롭게 코딩 가능)</p>

<p><strong>RAX는 EAX를 포함하고, EAX는 AX를 포함하고, AX는 AH + AL로 나뉜다.</strong></p>

<!-- post-image.html -->

<p><img src="/assets/images/assembly/2025-04-18-assembly1-start/image1.png" alt="image1" style="max-width: 100%;" /></p>

<ul>
  <li>*EAX/RAX (Accumulator Register, 누산기 레지스터)**산술, 논리 연산을 담당하는 레지스터로, 함수의 반환 값이 이 레지스터에 저장된다.
    <ul>
      <li><strong>EAX 레지스터의 종류</strong></li>
      <li>RAX: 64비트 (x86-64 아키텍처에서 확장된 EAX)</li>
      <li>EAX: 32비트 누산기 레지스터</li>
      <li>AX: 16비트의 EAX 레지스터 하위 부분</li>
      <li>AH: AX의 상위 8비트</li>
      <li>AL: AX의 하위 8비트</li>
    </ul>
  </li>
  <li>*EBX/RBX (Base Register, 베이스 레지스터)**메모리 주소를 저장하기 위해 사용되는 레지스터.종종 배열이나 문자열과 같은 데이터 구조에 접근하기 위한 기준 포인터로 사용된다.
    <ul>
      <li><strong>EBX 레지스터의 종류</strong></li>
      <li>RBX: 64비트 (x86-64 아키텍처에서 확장된 EBX)</li>
      <li>EBX: 32비트 베이스 레지스터</li>
      <li>BX: 16비트의 EBX 레지스터 하위 부분</li>
      <li>BH: BX의 상위 8비트</li>
      <li>BL: BX의 하위 8비트</li>
    </ul>
  </li>
  <li>*ECX/RCX (Count Register, 카운트 레지스터)**반복 작업에서 카운터 역할을 수행하는 레지스터이다.loop 명령어 사용 시 레지스터의 값을 하나씩 감소 시키며, 0이 될 때 까지 반복 작업을 수행한다.
    <ul>
      <li><strong>ECX 레지스터의 종류</strong></li>
      <li>RCX: 64비트 (x86-64 아키텍처에서 확장된 ECX)</li>
      <li>ECX: 32비트 카운트 레지스터</li>
      <li>CX: 16비트의 ECX 레지스터 하위 부분</li>
      <li>CH: CX의 상위 8비트</li>
      <li>CL: CX의 하위 8비트</li>
    </ul>
  </li>
  <li>*EDX/RDX (Data Register, 데이터 레지스터)**EAX 레지스터와 함께 사용하여 큰 수를 연산을 하거나, 그 결과를 저장할 수 있는 레지스터이다.64bit 더블 워드 연산을 수행할 때에도 사용 가능하다. (div, mul)
    <ul>
      <li><strong>EDX 레지스터의 종류</strong></li>
      <li>RDX: 64비트 (x86-64 아키텍처에서 확장된 EDX)</li>
      <li>EDX: 32비트 데이터 레지스터</li>
      <li>DX: 16비트의 EDX 레지스터 하위 부분</li>
      <li>DH: DX의 상위 8비트</li>
      <li>DL: DX의 하위 8비트</li>
    </ul>
  </li>
  <li>*ESI/RSI (Source Index, 소스 인덱스 레지스터)**데이터 복사, 문자열 연산, 입력/출력 처리 등의 작업에서 소스 데이터의 주소를 가리키는 데 사용된다.
    <ul>
      <li><strong>ESI 레지스터의 종류</strong></li>
      <li>RSI: 64비트 (x86-64 아키텍처에서 확장된 ESI)</li>
      <li>ESI: 32비트 소스 인덱스 레지스터</li>
      <li>SI: 16비트의 ESI 레지스터 하위 부분</li>
    </ul>
  </li>
  <li>*EDI/RDI (Destination Index, 목적지 인덱스 레지스터)**데이터 복사, 문자열 처리, 배열 조작 등의 작업에서 목적지 데이터의 메모리 주소를 가리키는데 사용된다.
    <ul>
      <li><strong>EDI 레지스터의 종류</strong></li>
      <li>RDI: 64비트 (x86-64 아키텍처에서 확장된 EDI)</li>
      <li>EDI: 32비트 목적지 인덱스 레지스터</li>
      <li>DI: 16비트의 EDI 레지스터 하위 부분</li>
    </ul>
  </li>
  <li>*ESP/RSP (Stack Pointer, 스택 포인터 레지스터)**프로그램의 스택 메모리 내에서, 현재 스택 최상단 주소를 저장하는 레지스터이다.함수 호출, 지역 변수 관리, 함수 내 데이터 저장 및 복구 등의 작업에서 필수적으로 사용된다.
    <ul>
      <li><strong>ESP 레지스터의 종류</strong></li>
      <li>RSP: 64비트 (x86-64 아키텍처에서 확장된 ESP)</li>
      <li>ESP: 32비트 스택 포인터 레지스터.</li>
      <li>SP: 16비트의 ESP 레지스터 하위 부분.</li>
    </ul>
  </li>
  <li>*EBP/RBP (Base Pointer, 베이스 포인터 레지스터)**함수 내의 지역 변수와 인자에 일관되고, 쉽게 접근하기 위해 사용되는 포인터 역할 레지스터이다.스택 내에서 접근할 부분의 메모리 주소를 저장한다.
    <ul>
      <li><strong>EBP 레지스터의 종류</strong></li>
      <li>RBP: 64비트 (x86-64 아키텍처에서 확장된 EBP).</li>
      <li>EBP: 32비트 베이스 포인터 레지스터.</li>
      <li>BP: 16비트의 EBP 레지스터 하위 부분.</li>
    </ul>
  </li>
  <li>*EIP/RIP (Instruction Pointer, 명령 포인터)**다음에 실행될 명령의 메모리 주소를 저장한다.
    <ul>
      <li><strong>EIP 레지스터의 종류</strong></li>
      <li>RIP: 64비트 (x86-64 아키텍처)</li>
      <li>EIP: 32비트</li>
    </ul>
  </li>
</ul>

<h2 id="windows-64bit-assembly-기본-구조"><strong>Windows 64bit Assembly 기본 구조</strong></h2>

<blockquote>
  <p>윈도우에서는 리눅스처럼 syscall 직접 안 씀.</p>

  <p>➔ 대신, <strong>Windows API</strong>를 “함수 호출” 방식으로 사용.</p>

</blockquote>

<p><strong>예시:</strong></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">MessageBoxA</code></li>
  <li><code class="language-plaintext highlighter-rouge">ExitProcess</code></li>
  <li><code class="language-plaintext highlighter-rouge">WriteFile</code></li>
  <li>이런 윈도우 기본 함수들을 호출하는 식!</li>
</ul>

<h2 id="windows-64bit-레지스터-호출-규약-중요"><strong>Windows 64bit 레지스터 호출 규약 (중요)</strong></h2>

<p>Windows 64bit는 호출 규칙이 정해져 있음: (<strong>Windows x64 Calling Convention</strong>)</p>

<table>
  <thead>
    <tr>
      <th>순서</th>
      <th>레지스터</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>RCX</td>
      <td>첫 번째 인자</td>
    </tr>
    <tr>
      <td>2</td>
      <td>RDX</td>
      <td>두 번째 인자</td>
    </tr>
    <tr>
      <td>3</td>
      <td>R8</td>
      <td>세 번째 인자</td>
    </tr>
    <tr>
      <td>4</td>
      <td>R9</td>
      <td>네 번째 인자</td>
    </tr>
    <tr>
      <td>이후</td>
      <td>스택(Stack)</td>
      <td>다섯 번째 인자부터는 스택에 저장</td>
    </tr>
  </tbody>
</table>

<p>즉, 함수 부를 때 <strong>인자(arguments)는 레지스터에 순서대로 넣는다!</strong></p>

<h2 id="함수-호출할-때-필요한-기본"><strong>함수 호출할 때 필요한 기본</strong></h2>

<ul>
  <li>함수 호출 전 인자를 RCX, RDX, R8, R9에 셋팅</li>
  <li>호출할 때 <code class="language-plaintext highlighter-rouge">call 함수이름</code></li>
  <li>스택은 항상 <strong>16바이트 정렬</strong> 되어야 함 (중요!)</li>
</ul>

<blockquote>
  <p>왜 16바이트 정렬? ➔ 성능 최적화 + AVX 같은 명령어 호환성 때문</p>

</blockquote>

<h2 id="메모리-구조-이해"><strong>메모리 구조 이해</strong></h2>

<!-- post-image.html -->

<p><img src="/assets/images/assembly/2025-04-18-assembly1-start/image2.png" alt="image2" style="max-width: 100%;" /></p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Text (Code) 영역</strong></td>
      <td>프로그램 코드 저장 (명령어들)</td>
    </tr>
    <tr>
      <td><strong>Data 영역</strong></td>
      <td>전역 변수 저장</td>
    </tr>
    <tr>
      <td><strong>BSS 영역</strong></td>
      <td>초기화 안 된 전역 변수 저장</td>
    </tr>
    <tr>
      <td><strong>Heap 영역</strong></td>
      <td>동적 할당 (new/malloc)</td>
    </tr>
    <tr>
      <td><strong>Stack 영역</strong></td>
      <td>함수 호출, 지역변수 저장, 복귀 주소 저장</td>
    </tr>
  </tbody>
</table>

<p><strong>Stack</strong>이 특히 중요</p>

<ul>
  <li>함수 부를 때마다 스택 위에 “새로운 공간”이 만들어지고</li>
  <li>함수 끝나면 그 공간이 “자동으로 사라짐”.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 코드(Code) 영역메모리의 코드(Code) 영역은 실행할 프로그램의 코드가 저장되는 영역으로 텍스트 영역이라고도 부른다. Hex파일이나 BIN 파일 메모리이다. CPU는 코드 영역에 저장된 명령어를 하나씩 가져가서 처리하게 되며 컴파일 타임에 결정되고 중간에 코드를 바꿀 수 없게 Read-Only로 지정되어 있다.

2. 데이터(Data) 영역메모리의 데이터(Data) 영역은 프로그램의 전역 변수(global), 정적(static) 변수, 배열(array), 구조체(structure)가 저장되는 영역이다. 초기화된 데이터는 data영역에 저장되며, 초기화되지 않는 데이터는 BSS(Block Stated Symbol) 영역에 저장된다. 이 영역은 실행 도중에 전역 변수가 변경될 수도 있으니 Read-Write로 지정되어 있다. 프로그램의 시작과 함께 할당되고, 프로그램 종료 시 소멸된다.

3. 힙(Heap) 영역메모리의 힙(Heap) 영역은 사용자가 집접 관리할 수 있는 '그기고 해야만 하는' 메모리 영역으로 사용자에 의해 메모리 공간이 동적으로 할당되고 해제된다. 힙 영역은 메모리의 낮은 주소에서 높은 주소의 방향으로 할당된다.

4. 스택(Stack) 영역메모리의 스택(Stack) 영역은 함수의 호출과 관계되는 지역 변수와 매개변수가 저장되는 영역으로  함수의 호출과 함께 할당되며, 함수의 호출이 완료되면 소멸된다. 이렇게 스택영역에 저장되는 함수의 호출 정보를 스택 프레임이라고 한다. 스택 영역은 푸시(push) 동작으로 데이터를 저장하고 , 팝(pop) 동작으로 데이터를 인출한다. 이러한 스택은 후입선출(LIFO, Last-In First-Out) 방식에 따라 동작하므로, 가장 늦게 저장된 데이터가 가장 먼저 인출된다. 스택 영역은 메모리의 높은 주소에서 낮은 주소의 방향으로 할당된다.

</code></pre></div></div>

<h2 id="64bit-메모리-구조"><strong>64bit 메모리 구조</strong></h2>

<p>64bit는 메모리 주소 자체가 64bit.</p>

<p>(= 2의 64승 만큼 메모리 접근할 수 있음. <strong>거의 무한대</strong>)</p>

<p>나머지 구조는 비슷하지만:</p>

<ul>
  <li><strong>스택(Stack)</strong> 여전히 사용</li>
  <li><strong>힙(Heap)</strong> 여전히 malloc/new로 동적 할당</li>
  <li><strong>데이터 영역(Data)</strong> 여전히 전역 변수 저장</li>
</ul>

<p><strong>다만!</strong> 64bit에서는 스택 정렬(16바이트 alignment)이 필수. (뒤에 함수 호출할 때 배울 예정)</p>

<h2 id="레지스터와-메모리-관계-요약"><strong>레지스터와 메모리 관계 요약</strong></h2>

<ul>
  <li>레지스터는 <strong>초고속 임시 저장소</strong> (작고 빠름)</li>
  <li>메모리는 <strong>크고 느림</strong> (하지만 양은 많음)</li>
  <li>속도: 레지스터 » 캐시 » 메모리(RAM)</li>
</ul>

<p>그래서 Assembly 코드는 <strong>“레지스터를 최대한 많이 쓰고”</strong>, <strong>“필요할 때만 메모리 접근”</strong> 하려 함.</p>

<h2 id="실행-흐름-예제"><strong>실행 흐름 예제</strong></h2>

<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">mov</span> <span class="nb">eax</span><span class="p">,</span> <span class="mi">5</span>      <span class="c1">; eax에 5 저장</span>
<span class="nf">mov</span> <span class="nb">ebx</span><span class="p">,</span> <span class="mi">10</span>     <span class="c1">; ebx에 10 저장</span>
<span class="nf">add</span> <span class="nb">eax</span><span class="p">,</span> <span class="nb">ebx</span>    <span class="c1">; eax = eax + ebx (5 + 10 = 15)</span>

</code></pre></div></div>

<p>실행 흐름:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">eax = 5</code></li>
  <li><code class="language-plaintext highlighter-rouge">ebx = 10</code></li>
  <li><code class="language-plaintext highlighter-rouge">eax = eax + ebx</code> → 결과 15가 eax에 들어감</li>
</ul>

<h2 id="운영체제-차이-요약"><strong>운영체제 차이 요약</strong></h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>리눅스</th>
      <th>윈도우</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>호출 방식</td>
      <td>syscall 직접 호출</td>
      <td>Windows API 호출</td>
    </tr>
    <tr>
      <td>시스템콜 번호 필요</td>
      <td>O</td>
      <td>X</td>
    </tr>
    <tr>
      <td>인자 전달 방식</td>
      <td>레지스터 + 스택</td>
      <td>RCX, RDX, R8, R9 순서</td>
    </tr>
    <tr>
      <td>스택 정렬</td>
      <td>16바이트 필수 아님</td>
      <td>16바이트 필수</td>
    </tr>
  </tbody>
</table>

<h2 id="요약"><strong>요약</strong></h2>

<ul>
  <li>Windows에서는 syscall 사용 안 함.</li>
  <li>무조건 Windows API (<code class="language-plaintext highlighter-rouge">WriteFile</code>, <code class="language-plaintext highlighter-rouge">ExitProcess</code>, <code class="language-plaintext highlighter-rouge">GetStdHandle</code>, 등등) 호출하는 구조.</li>
  <li>인자는 RCX, RDX, R8, R9에 순서대로 넣는다.</li>
  <li>스택 16바이트 정렬 <strong>필수</strong>.</li>
</ul>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="assembly" /><category term="nasm" /><category term="assembly" /><summary type="html"><![CDATA[assembly #1]]></summary></entry><entry><title type="html">GitHub Blog 목차 설정</title><link href="https://ruyshy.github.io/blog/blog8-toc/" rel="alternate" type="text/html" title="GitHub Blog 목차 설정" /><published>2025-04-18T00:00:00+09:00</published><updated>2025-04-18T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog8-toc</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog8-toc/"><![CDATA[<p>GitHub Blog #8</p>

<h2 id="목차-설정-방법">목차 설정 방법</h2>

<p>목차 설정 코드:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>toc: true
toc_sticky: true
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">toc</code>는 목차, <code class="language-plaintext highlighter-rouge">toc_sticky</code>는 스크롤 따라가기</p>

<p>게시글 설정 예시:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">title</span><span class="pi">:</span> <span class="s2">"</span><span class="s">GitHub</span><span class="nv"> </span><span class="s">Blog</span><span class="nv"> </span><span class="s">Start"</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2025-04-10</span>
<span class="na">tags</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">GitHubBlog</span><span class="pi">]</span>
<span class="na">toc</span><span class="pi">:</span> <span class="kc">true</span>
<span class="na">toc_sticky</span><span class="pi">:</span> <span class="kc">true</span>
<span class="nn">---</span>
</code></pre></div></div>

<p>게시글 전체 적용 예시 (_config.yml):</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="pi">-</span> <span class="na">scope</span><span class="pi">:</span>
      <span class="na">path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">posts</span>
    <span class="na">values</span><span class="pi">:</span>
      <span class="na">layout</span><span class="pi">:</span> <span class="s">single</span>
      <span class="na">author_profile</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">toc</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">toc_sticky</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">comments</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">share</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">related</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">sidebar</span><span class="pi">:</span>
        <span class="na">nav</span><span class="pi">:</span> <span class="s2">"</span><span class="s">main"</span>
</code></pre></div></div>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #8]]></summary></entry><entry><title type="html">GitHub Blog 이미지 커스텀 플로그인</title><link href="https://ruyshy.github.io/blog/blog9-Image-plugin/" rel="alternate" type="text/html" title="GitHub Blog 이미지 커스텀 플로그인" /><published>2025-04-18T00:00:00+09:00</published><updated>2025-04-18T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog9-Image-plugin</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog9-Image-plugin/"><![CDATA[<p>GitHub Blog #9</p>

<h2 id="기존-방식-과-태그-플러그인-비교">기존 방식 과 태그 플러그인 비교</h2>

<p>기존에 이미지 불러오는 방식:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{% include post-image.html name="image.png" alt="image" %}
</code></pre></div></div>

<p>플러그인 적용한 방식:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{% img "image1.png", "tag image test" %}
</code></pre></div></div>

<p>or</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{% img "image1.png" %}
</code></pre></div></div>

<h2 id="pluginsimg_tagrb-코드-추가">\plugins\img_tag.rb 코드 추가</h2>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">module</span> <span class="nn">Jekyll</span>
  <span class="k">class</span> <span class="nc">ImgTag</span> <span class="o">&lt;</span> <span class="no">Liquid</span><span class="o">::</span><span class="no">Tag</span>
    <span class="k">def</span> <span class="nf">initialize</span><span class="p">(</span><span class="n">tag_name</span><span class="p">,</span> <span class="n">markup</span><span class="p">,</span> <span class="n">tokens</span><span class="p">)</span>
      <span class="k">super</span>
      <span class="vi">@markup</span> <span class="o">=</span> <span class="n">markup</span><span class="p">.</span><span class="nf">strip</span>
    <span class="k">end</span>

    <span class="k">def</span> <span class="nf">render</span><span class="p">(</span><span class="n">context</span><span class="p">)</span>
      <span class="n">args</span> <span class="o">=</span> <span class="vi">@markup</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="s2">","</span><span class="p">).</span><span class="nf">map</span><span class="p">(</span><span class="o">&amp;</span><span class="ss">:strip</span><span class="p">)</span>
      <span class="n">img_name</span> <span class="o">=</span> <span class="n">sanitize</span><span class="p">(</span><span class="n">args</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
      <span class="n">alt_text</span> <span class="o">=</span> <span class="n">args</span><span class="p">.</span><span class="nf">length</span> <span class="o">&gt;</span> <span class="mi">1</span> <span class="p">?</span> <span class="n">sanitize</span><span class="p">(</span><span class="n">args</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span> <span class="p">:</span> <span class="n">img_name</span>

      <span class="n">page</span> <span class="o">=</span> <span class="n">context</span><span class="p">.</span><span class="nf">registers</span><span class="p">[</span><span class="ss">:page</span><span class="p">]</span> <span class="o">||</span> <span class="p">{}</span>
      <span class="n">page_path</span> <span class="o">=</span> <span class="n">page</span><span class="p">[</span><span class="s1">'path'</span><span class="p">]</span> <span class="o">||</span> <span class="s1">''</span>

      <span class="n">parts</span> <span class="o">=</span> <span class="n">page_path</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="s1">'/'</span><span class="p">)</span>

      <span class="n">idx</span> <span class="o">=</span> <span class="n">parts</span><span class="p">.</span><span class="nf">find_index</span><span class="p">(</span><span class="s2">"_posts"</span><span class="p">)</span>
      <span class="k">if</span> <span class="n">idx</span> <span class="o">&amp;&amp;</span> <span class="n">parts</span><span class="p">.</span><span class="nf">length</span> <span class="o">&gt;</span> <span class="n">idx</span> <span class="o">+</span> <span class="mi">2</span>
        <span class="n">category</span> <span class="o">=</span> <span class="n">parts</span><span class="p">[</span><span class="n">idx</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span>
        <span class="n">post_filename</span> <span class="o">=</span> <span class="n">parts</span><span class="p">[</span><span class="n">idx</span> <span class="o">+</span> <span class="mi">2</span><span class="p">]</span>
      <span class="k">else</span>
        <span class="n">category</span> <span class="o">=</span> <span class="s2">""</span>
        <span class="n">post_filename</span> <span class="o">=</span> <span class="s2">"unknown-post.md"</span>
      <span class="k">end</span>

      <span class="n">post_name</span> <span class="o">=</span> <span class="no">File</span><span class="p">.</span><span class="nf">basename</span><span class="p">(</span><span class="n">post_filename</span><span class="p">,</span> <span class="no">File</span><span class="p">.</span><span class="nf">extname</span><span class="p">(</span><span class="n">post_filename</span><span class="p">))</span>

      <span class="n">img_src</span> <span class="o">=</span> <span class="s2">"/assets/images/</span><span class="si">#{</span><span class="n">category</span><span class="si">}</span><span class="s2">/</span><span class="si">#{</span><span class="n">post_name</span><span class="si">}</span><span class="s2">/</span><span class="si">#{</span><span class="n">img_name</span><span class="si">}</span><span class="s2">"</span>

      <span class="sx">%(&lt;img src="</span><span class="si">#{</span><span class="n">img_src</span><span class="si">}</span><span class="sx">" alt="</span><span class="si">#{</span><span class="n">alt_text</span><span class="si">}</span><span class="sx">" style="max-width:100%;"&gt;)</span>
    <span class="k">end</span>

    <span class="kp">private</span>

    <span class="k">def</span> <span class="nf">sanitize</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
      <span class="n">text</span><span class="p">.</span><span class="nf">gsub</span><span class="p">(</span><span class="s1">'"'</span><span class="p">,</span> <span class="s1">''</span><span class="p">).</span><span class="nf">gsub</span><span class="p">(</span><span class="s2">"'"</span><span class="p">,</span> <span class="s1">''</span><span class="p">)</span>
    <span class="k">end</span>
  <span class="k">end</span>
<span class="k">end</span>

<span class="no">Liquid</span><span class="o">::</span><span class="no">Template</span><span class="p">.</span><span class="nf">register_tag</span><span class="p">(</span><span class="s1">'img'</span><span class="p">,</span> <span class="no">Jekyll</span><span class="o">::</span><span class="no">ImgTag</span><span class="p">)</span>

</code></pre></div></div>

<h2 id="주의사항">주의사항</h2>
<p>해당 글 처럼 블로그 예시를 작성하기 위해</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> {% raw %}
 {% img "test.png" %}
 {% endraw %}
</code></pre></div></div>
<p>liquid 문법의 raw tag를 사용해야한다.</p>

<h2 id="예시-사용법">예시 사용법</h2>

<table>
  <thead>
    <tr>
      <th>작성</th>
      <th>실제 결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">{% img "test.png" %}</code></td>
      <td><code class="language-plaintext highlighter-rouge">&lt;img src="/assets/images/xxx/xxx/test.png" alt="test.png" style="max-width:100%;"&gt;</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">{% img "test.png", "설명" %}</code></td>
      <td><code class="language-plaintext highlighter-rouge">&lt;img src="/assets/images/xxx/xxx/test.png" alt="설명" style="max-width:100%;"&gt;</code></td>
    </tr>
  </tbody>
</table>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #9]]></summary></entry><entry><title type="html">GitHub Blog 폰트 설정</title><link href="https://ruyshy.github.io/blog/blog7-font/" rel="alternate" type="text/html" title="GitHub Blog 폰트 설정" /><published>2025-04-17T00:00:00+09:00</published><updated>2025-04-17T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog7-font</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog7-font/"><![CDATA[<p>GitHub Blog #7</p>

<p>여러 폰트 사이트가 있지만 저는 구글 폰트 사이트를 예시로 사용하겠습니다.</p>

<p><a href="https://fonts.google.com/">https://fonts.google.com/</a></p>

<p>저는 폰트를 다운 받아서 진행했습니다.
Dongle Font(Dongle-Regular.ttf)를 사용하고 있습니다.</p>

<p>_sass\minimal-mistakes_variables.scss 추가</p>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$dongle</span><span class="p">:</span> <span class="s1">'Dongle'</span><span class="o">,</span> <span class="o">-</span><span class="n">apple-system</span><span class="o">,</span> <span class="n">BlinkMacSystemFont</span><span class="o">,</span> <span class="s2">"Segoe UI"</span><span class="o">,</span> <span class="nb">sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
</code></pre></div></div>

<p>수정해야 할 부분:</p>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$global-font-family</span><span class="p">:</span> <span class="nv">$sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
<span class="nv">$header-font-family</span><span class="p">:</span> <span class="nv">$sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
</code></pre></div></div>

<p>수정할 코드:</p>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$global-font-family</span><span class="p">:</span> <span class="nv">$dongle</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
<span class="nv">$header-font-family</span><span class="p">:</span> <span class="nv">$dongle</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
</code></pre></div></div>

<p>수정해야 할 부분:</p>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* system typefaces */</span>
<span class="nv">$serif</span><span class="p">:</span> <span class="n">Dongle</span><span class="o">,</span> <span class="n">Georgia</span><span class="o">,</span> <span class="n">Times</span><span class="o">,</span> <span class="nb">serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
<span class="nv">$sans-serif</span><span class="p">:</span> <span class="o">-</span><span class="n">apple-system</span><span class="o">,</span> <span class="n">BlinkMacSystemFont</span><span class="o">,</span> <span class="s2">"Dongle"</span><span class="o">,</span><span class="s2">"Roboto"</span><span class="o">,</span> <span class="s2">"Segoe UI"</span><span class="o">,</span>
  <span class="s2">"Helvetica Neue"</span><span class="o">,</span> <span class="s2">"Lucida Grande"</span><span class="o">,</span> <span class="n">Arial</span><span class="o">,</span> <span class="nb">sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
<span class="nv">$monospace</span><span class="p">:</span> <span class="n">Monaco</span><span class="o">,</span> <span class="n">Consolas</span><span class="o">,</span> <span class="s2">"Lucida Console"</span><span class="o">,</span> <span class="nb">monospace</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>

<span class="cm">/* sans serif typefaces */</span>
<span class="nv">$sans-serif-narrow</span><span class="p">:</span> <span class="nv">$sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
<span class="nv">$helvetica</span><span class="p">:</span> <span class="s2">"Dongle"</span><span class="o">,</span> <span class="n">Helvetica</span><span class="o">,</span> <span class="s2">"Helvetica Neue"</span><span class="o">,</span> <span class="n">Arial</span><span class="o">,</span> <span class="nb">sans-serif</span> <span class="o">!</span><span class="nb">default</span><span class="p">;</span>
</code></pre></div></div>

<p>위 처럼 Dongle를 추가해주었습니다.</p>

<p>\assets\css\main.scss 추가</p>

<div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">@font-face</span> <span class="p">{</span>
    <span class="nl">font-family</span><span class="p">:</span> <span class="s1">'Dongle'</span><span class="p">;</span>
    <span class="nl">src</span><span class="p">:</span> <span class="sx">url('/assets/fonts/Dongle-Regular.ttf')</span> <span class="nf">format</span><span class="p">(</span><span class="s1">'truetype'</span><span class="p">);</span>
    <span class="nl">font-weight</span><span class="p">:</span> <span class="nb">normal</span><span class="p">;</span>
    <span class="nl">font-style</span><span class="p">:</span> <span class="nb">normal</span><span class="p">;</span>
  <span class="p">}</span>
</code></pre></div></div>

<p>저는 기본 폰트가 이쁜거같아서 다시 원상 복구 했습니다.</p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #7]]></summary></entry><entry><title type="html">GitHub Blog 로컬 서버 설정</title><link href="https://ruyshy.github.io/blog/blog2-local-server/" rel="alternate" type="text/html" title="GitHub Blog 로컬 서버 설정" /><published>2025-04-16T00:00:00+09:00</published><updated>2025-04-16T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog2-local-server</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog2-local-server/"><![CDATA[<p>GitHub Blog #2</p>

<h2 id="필요한-준비물">필요한 준비물</h2>

<p><a href="https://www.ruby-lang.org/ko/">https://www.ruby-lang.org/ko/</a></p>

<h2 id="gemfile">Gemfile</h2>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Gemfile</span>
<span class="n">source</span> <span class="s2">"https://rubygems.org"</span>

<span class="n">gem</span> <span class="s2">"jekyll"</span><span class="p">,</span> <span class="s2">"~&gt; 4.3.2"</span>
<span class="n">gem</span> <span class="s2">"jekyll-paginate"</span>
<span class="n">gem</span> <span class="s2">"jekyll-sitemap"</span>
<span class="n">gem</span> <span class="s2">"jekyll-feed"</span>
<span class="n">gem</span> <span class="s2">"jekyll-include-cache"</span>
<span class="n">gem</span> <span class="s2">"jekyll-gist"</span>
<span class="n">gem</span> <span class="s2">"tzinfo"</span>
<span class="n">gem</span> <span class="s2">"tzinfo-data"</span>
</code></pre></div></div>

<p>Ruby를 설치해줍니다. 저의 경우 ruby3.3.8 의 버전을 설치하여 진행했습니다.</p>

<ol>
  <li>Gemfile을 위와 같이 수정을 진행합니다.</li>
  <li>자기 자신의 Github Blog가 있는 로컬 폴더에서 cmd를 실행시키거나 cmd를 실행시켜 예시) cd C:[ruyshy.github.io](http://ruyshy.github.io/)(경로) 통해 이동해줍니다.</li>
  <li><code class="language-plaintext highlighter-rouge">bundle install</code> 명령어를 통해 위와 같은 종속성을 설치를 진행합니다.</li>
  <li>설치가 정상적으로 진행되었을 때, Gemfile.lock 파일이 생성이 됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">bundle exec jekyll serve</code> 을 통해 Local Server를 열어줍니다.</li>
  <li>정상적으로 작동했으면 아래와 같은 그림의 텍스트가 cmd에서 실행됩니다.</li>
</ol>

<!-- post-image.html -->

<p><img src="/assets/images/blog/2025-04-16-blog2-local-server/image.png" alt="image" style="max-width: 100%;" /></p>

<ol>
  <li>Server address : <a href="http://127.0.0.1:4000/">http://127.0.0.1:4000/</a> 에 접속하여 실시간으로 수정을 진행하며 확인할 수 있습니다.</li>
</ol>

<p>서버를 켜둔 상태에서 서버를 종료하려면 ctrl-c를 눌러 y를 입력해 서버를 종료합니다.</p>

<p><code class="language-plaintext highlighter-rouge">bundle exec jekyll clean</code> 해당 명령어로 이전 빌드 결과가 꼬였거나, 레이아웃, 설정파일 변경 후 캐시를 무시하고 새로 빌드하고싶을 때 사용합니다.</p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #2]]></summary></entry><entry><title type="html">GitHub Blog 빌드&amp;amp;릴리즈 설정</title><link href="https://ruyshy.github.io/blog/blog3-build-release/" rel="alternate" type="text/html" title="GitHub Blog 빌드&amp;amp;릴리즈 설정" /><published>2025-04-16T00:00:00+09:00</published><updated>2025-04-16T00:00:00+09:00</updated><id>https://ruyshy.github.io/blog/blog3-build-release</id><content type="html" xml:base="https://ruyshy.github.io/blog/blog3-build-release/"><![CDATA[<p>GitHub Blog #3</p>

<h2 id="github에-action기능-알아보기">GitHub에 Action기능 알아보기</h2>

<p>GitHub Action 문서 : <a href="https://docs.github.com/ko/actions">https://docs.github.com/ko/actions</a></p>

<p>GitHub Actions는 <strong>CI/CD(지속적 통합/배포)</strong>를 위한 자동화된 작업(Workflow)을 만들어주는 도구입니다. 블로그 자동 배포와 코드 빌드 등의 작업을 자동으로 처리할 수 있습니다.</p>

<h2 id="github-actions-개념-정리">GitHub Actions 개념 정리</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Workflow</strong></td>
      <td>자동화 작업의 전체 흐름 (YAML 파일로 설정)</td>
    </tr>
    <tr>
      <td><strong>Job</strong></td>
      <td>Workflow 내에서 실행되는 작업 단위</td>
    </tr>
    <tr>
      <td><strong>Step</strong></td>
      <td>Job 안에서 실행되는 세부 명령</td>
    </tr>
    <tr>
      <td><strong>Action</strong></td>
      <td>재사용 가능한 명령 묶음 (남이 만든 것도 쉽게 사용 가능)</td>
    </tr>
    <tr>
      <td><strong>Runner</strong></td>
      <td>GitHub 서버 or 네가 직접 설정한 컴퓨터에서 실행됨</td>
    </tr>
  </tbody>
</table>

<h2 id="github-workflow-생성-방법">GitHub Workflow 생성 방법</h2>

<!-- post-image.html -->

<p><img src="/assets/images/blog/2025-04-16-blog3-build-release/image1.png" alt="image1" style="max-width: 100%;" /></p>

<p>Action을 생성할 저장소에 들어가 Action을 눌러 Action페이지로 이동합니다.</p>

<!-- post-image.html -->

<p><img src="/assets/images/blog/2025-04-16-blog3-build-release/image2.png" alt="image2" style="max-width: 100%;" /></p>

<p>New workflow을 눌러 .yml을 작성해줍니다.</p>

<h2 id="현재-blog-workflow">현재 Blog Workflow</h2>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Build and Deploy Jekyll</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">ph-pages</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">write</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build-deploy</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout source</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">ref</span><span class="pi">:</span> <span class="s">ph-pages</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Setup Ruby</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">ruby/setup-ruby@v1</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">ruby-version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3.3'</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install dependencies</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">gem install bundler</span>
          <span class="s">bundle install</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Clean previous builds</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">bundle exec jekyll clean</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build Jekyll site</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">JEKYLL_ENV=production bundle exec jekyll build --future</span>


      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to gh-pages</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">peaceiris/actions-gh-pages@v3</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">github_token</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">publish_branch</span><span class="pi">:</span> <span class="s">gh-pages</span>
          <span class="na">publish_dir</span><span class="pi">:</span> <span class="s">./_site</span>
</code></pre></div></div>

<h2 id="코드-진행-설명">코드 진행 설명</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ph-pages</code> 브랜치에 푸시하면,</li>
  <li>GitHub 서버에서 자동으로 Ruby 세팅하고,</li>
  <li>Jekyll로 블로그 빌드하고,</li>
  <li>결과물(<code class="language-plaintext highlighter-rouge">_site</code> 폴더)을 <code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치로 배포함.</li>
</ul>

<h2 id="github-설정">GitHub 설정</h2>

<p><img src="/assets/images/blog/2025-04-16-blog3-build-release/image3.png" alt="image3.png" style="max-width:100%;" /></p>

<p>Branch 를 gh-pages 로 설정해줘야 합니다!!!</p>

<h2 id="사용할-수-있는-곳">사용할 수 있는 곳</h2>

<ul>
  <li>블로그 자동 배포 (Jekyll, Hugo 등)</li>
  <li>코드 테스트 자동 실행</li>
  <li>PR 올릴 때 자동으로 포맷 검사</li>
  <li>릴리즈 빌드 자동 생성</li>
  <li>정적 웹사이트 자동 배포 (React, Vue 등)</li>
</ul>

<h2 id="위-코드-주의점-및-확인해야-할-부분">위 코드 주의점 및 확인해야 할 부분</h2>

<ol>
  <li>워크플로우 파일 위치 확인
    <ol>
      <li>워크플로우 파일이 반드시 <strong><code class="language-plaintext highlighter-rouge">.github/workflows/</code></strong> 폴더 안에 있어야 함.</li>
    </ol>

    <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code> .github/
   workflows/
     deploy.yml ← 여기에 있어야 작동함!
    
</code></pre></div>    </div>
  </li>
  <li><code class="language-plaintext highlighter-rouge">ph-pages</code> 브랜치로 <strong>푸시가 실제로 있었는지</strong>
    <ol>
      <li>GitHub Action은 <code class="language-plaintext highlighter-rouge">on: push</code> 조건에 따라 실행됩니다. <code class="language-plaintext highlighter-rouge">ph-pages</code> 브랜치에 push해야만 작동합니다.
        <ol>
          <li>GitHub Desktop을 사용하는 경우, 커밋 후에 <strong>실제로 push까지 완료했는지 확인</strong>하세요.</li>
          <li>파일만 수정하고 커밋하지 않으면 작동하지 않습니다.</li>
          <li><code class="language-plaintext highlighter-rouge">git push origin ph-pages</code> 명령이 실행된 상태여야 합니다.</li>
        </ol>
      </li>
    </ol>
  </li>
  <li><code class="language-plaintext highlighter-rouge">.jekyll-cache</code>나 <code class="language-plaintext highlighter-rouge">_site</code>가 Git으로 추적되고 있는지
    <ol>
      <li>이건 종종 문제가 되진 않지만, 만약 <code class="language-plaintext highlighter-rouge">_site/</code>가 git에 들어있고 <code class="language-plaintext highlighter-rouge">.gitignore</code> 안 되어 있으면 충돌 생길 수 있음.</li>
      <li><code class="language-plaintext highlighter-rouge">.gitignore</code>에 아래 내용이 있는지 확인:</li>
    </ol>

    <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code> _site/
 .jekyll-cache/
</code></pre></div>    </div>
  </li>
  <li>GitHub 페이지 설정 확인
    <ol>
      <li><code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치를 페이지 소스로 지정했는지?
        <ol>
          <li><a href="https://github.com/%EC%82%AC%EC%9A%A9%EC%9E%90%EB%AA%85/%EB%A0%88%ED%8F%AC%EB%AA%85/settings/pages">Repository → Settings → Pages</a> 에 가서</li>
          <li><strong>Source</strong>: <code class="language-plaintext highlighter-rouge">gh-pages</code> 선택</li>
          <li><strong>Folder</strong>: <code class="language-plaintext highlighter-rouge">/ (root)</code> 또는 <code class="language-plaintext highlighter-rouge">/docs</code> 말고 <code class="language-plaintext highlighter-rouge">/</code>로 돼야 함</li>
        </ol>
      </li>
    </ol>
  </li>
</ol>

<p>push 없이도 수동 실행 가능하게 하려면 아래처럼 수정:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">ph-pages</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span> <span class="c1"># 수동 실행 가능</span>

</code></pre></div></div>
<p>GitHub UI 상에서 “Run workflow” 버튼이 생성됩니다.</p>]]></content><author><name>ruyshy</name><email>ruyshyttr@gmail.com</email></author><category term="blog" /><category term="GitHubBlog" /><summary type="html"><![CDATA[GitHub Blog #3]]></summary></entry></feed>